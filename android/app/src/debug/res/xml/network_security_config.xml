<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug-only: HTTPS by default, with cleartext exceptions for local
         development (Flutter web/desktop emulator + Firebase emulator suite).
         Release builds use the stricter variant in src/main/res/xml/. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext traffic for localhost during development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>
</network-security-config>
