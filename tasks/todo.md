# Sprint Backlog

## Sprint: mvvm-tag-routing + cooking-card-deadpath — close the two clean Tier-A code-hygiene follow-ups this iteration can fully vouch for (2 tickets) — 2026-06-14 (iter-162)

The iter-161 sprint shipped fully in commit `130d9547c` (BUT-1303 SSRF host-guard URL-import coverage → Done; BUT-1302 veckomeny cooking-session-card hide-branch widget tests → Done; follow-up BUT-1305 filed). Working tree clean, no carry-forward.

Backlog re-read: 94 open Backlog + 4 Todo (BUT-530/1149/431/1050, all carried tech-debt/perf) + 0 In Progress + 0 Triage. The genuinely-clean Tier-A code slice stays thin — the overwhelming majority are launch-gated/ops (BUT-415/420/451/486/492/646/731/813/814/819/880/890/1166/1229/1239/1240...), monetization/marketing scaffolding (BUT-644/650/658/661/664/668/672/683/685/686/916/...), epics (BUT-907/1156/1157/1159/520), SDK-blocked dependency bumps (BUT-435/503/507/509/554/820/1218), explicitly-deferred-by-own-body YAGNI follow-ups (BUT-1248/1067/1011), or product/UX decisions that are Malin's call (BUT-976/941/945/1179/1261/1290/1273...). Two honest, fully-disjoint Tier-A items remain that I can vouch for — both code-hygiene follow-ups filed by recent verifiers, both confirmed against current code at Step-0 (BUT-1304 MVVM routing, BUT-1305 dead-path). Not padding to N=6 with deferred/debatable work would be false mandate; the rest is surfaced under "Needs you".

### Agent A: mvvm-tag-routing — move the editTags persistence call into the viewmodel
- [ ] **A1. Add `updateRecipeTagOverrides()` to RecipeDetailViewModel; call it from the view** `[Tier A]` (BUT-1304) — `lib/viewmodels/recipe_detail_viewmodel.dart` (new method mirroring the optimistic-update + `_recipeService.updateRecipe` + revert-on-failure + notifyListeners pattern at `rateRecipe` ~line 469), `lib/views/recipe_detail_view.dart` (editTags case lines 894-901: replace direct `ServiceLocator.get<UnifiedRecipeService>().updateRecipe(updated)` + `viewModel.updateRecipe(updated)` with the single VM call), `test/unit/viewmodels/recipe_detail_viewmodel_test.dart` (extend).
  - Acceptance: `recipe_detail_view.dart` editTags case no longer calls `UnifiedRecipeService.updateRecipe` directly — it routes through a `RecipeDetailViewModel` method (no `ServiceLocator.get<UnifiedRecipeService>()` in that case body) · The new VM method persists the tagOverrides via the recipe service AND reverts local state if the service returns failure (parity with the existing `rateRecipe` optimistic pattern) · Existing error-on-failure user feedback is preserved (a failed save still surfaces an error, not a silent success) · A unit test drives the new method on a personal recipe and asserts the service was called with the overridden recipe and that a service-failure path reverts/throws
  - requiresPlanMode: false (Low pri, Improvement, single area, no security/cross-module surface)

### Agent B: cooking-card-deadpath — resolve the unreachable presence-stream-null branch
- [ ] **B1. Cover or delete the `stream == null` hide branch in VeckomenyCookingSessionCard** `[Tier A]` (BUT-1305) — `lib/widgets/menu/veckomeny_selection_widgets.dart` (`_VeckomenyCookingSessionCardState.build` line 139 `if (stream == null) return const SizedBox.shrink();`), `test/widget/widgets/menu/veckomeny_cooking_session_card_test.dart` (extend if the path is reachable). Step-0 confirmed the branch as likely dead: `CookingSessionStreamHolder.refresh` only yields a null stream when `groupIds` is empty, and `groups.isEmpty` already short-circuits at line 135. Decide: prove reachable → add a test asserting `SizedBox.shrink`; prove dead → replace the branch with an `assert`/explanatory comment so it is not counted as untested coverage. Do NOT weaken or delete any of the 5 existing passing tests.
  - Acceptance: Either (a) a new widget test drives the card into a genuine `stream == null` state and asserts `SizedBox.shrink`, OR (b) the branch is replaced by an assert/comment documenting it as provably unreachable given the `groups.isEmpty` short-circuit — exactly one of the two, with the reasoning visible in the diff · The 5 pre-existing tests in the file still pass unchanged (no assertion weakened) · `flutter analyze` is clean on the changed file(s) · If (b) is chosen, the widget's observable hide/show behavior for empty-groups and populated-stream cases is unchanged (the short-circuit at line 135 still returns `SizedBox.shrink` for the empty case)
  - requiresPlanMode: false (Low pri, test-gap/tech-debt, single widget, test-only or dead-code removal)

### Needs you (not built — flagged for your call)
- **BUT-1248** (Low, backend/tech-debt) — schemaVersion migration-dispatch + backfill framework. Recommendation: **leave deferred** — the ticket's own body says building it now is "speculative dead code"; trigger is the first real v2 breaking schema change (e.g. subscription fields). The v1 field foundation already shipped.
- **BUT-1067** (Low, Improvement) — backfill `description` metadata on ~3000 remaining ARB i18n keys. Recommendation: **leave deferred** — own body says "YAGNI until then, sleep"; the value (translator context) only matters once a third locale or external translator is actually engaged. Mechanical ~10-ticket area-split when triggered.
- **BUT-1011** (Low, backend) — async status-polling for very large account-deletion. Recommendation: **leave deferred** — premature optimization; the 9-min budget covers heavy users. File-triggered only if production telemetry shows real `deadline-exceeded` timeouts.
- **BUT-1273** (Medium) — URL-import recipe-index/listing-page link expansion. Recommendation: **build-review / your call on scope** — this is net-new behavior (detect a listing page, fetch it, scrape individual recipe links, enqueue them), not a bug fix. New external-fetch surface and a real UX decision about what counts as a "listing page". Worth doing eventually, but it's a feature build you should green-light, not an auto-pick.
- **BUT-1299** (Low) — confirm BUT-675 close-out for the nextPage() persistence comment. Recommendation: **close as no-op** — pure bookkeeping; BUT-675 returns "Entity not found" (already closed/archived) and the WHY comment already exists in HEAD with zero diff. No code or behavior change to make.
- **BUT-1261 / BUT-1290 / BUT-1268 / BUT-884 / BUT-1169 / BUT-840 / BUT-1288 / BUT-1179 / BUT-976 / BUT-945 / BUT-941** — carried product/UX/ops-decision tickets from prior iterations (conflict-diff semantics, activity-feed banner fate, cold-start split meta-decision, progress-indicator migration, legacy-shopping-constant backfill, Algolia mirror, iOS timer doc-confirm, manual QA pass, per-step images, rejoin UX, multi-share). Each is either Malin's product call, needs ops/prod access, or is a debatable behavior change — not auto-pick mandate.

### Needs you (Tier D — true ops/launch blockers, not worked)
- BUT-1229 / BUT-814 / BUT-880 / BUT-819 / BUT-821 / BUT-813 / BUT-1166 / BUT-486 / BUT-451 / BUT-492 / BUT-731 / BUT-646 / BUT-415 / BUT-420 / BUT-890 / BUT-594 / BUT-714 / BUT-1239 / BUT-1240 — deploy/console/cert-capture/store-submission/CI-lane/secrets the loop can't reach.

### Obsolete (done in git, still open in Linear)
- (none — iter-161's BUT-1302/1303 correctly Done via `130d9547c`; follow-up BUT-1305 correctly filed and now selected this iteration)

### Post-Sprint Steps
- [ ] Run `dart analyze --fatal-infos`
- [ ] Run `test/unit/viewmodels/recipe_detail_viewmodel_test.dart` + `test/widget/widgets/menu/veckomeny_cooking_session_card_test.dart`
- [ ] Commit, push to main
- [ ] Update Linear: BUT-1304 → Done (Tier A, MVVM routing, test-verifiable); BUT-1305 → Done (Tier A, test-only or dead-code removal). Leave the "Needs you" tickets untouched (flagged for Malin).

---
## ARCHIVED — iter-161 (import-ssrf-parity + veckomeny-cooking-card-tests: BUT-1303 SSRF host-guard coverage + BUT-1302 cooking-session-card hide-branch tests → BUT-1305 filed — `130d9547c`) · iter-160 (recipe-detail-import-seam + veckomeny-decomp: BUT-1300/1301/1277 Done + BUT-1258 In Review — `8b91c165b`) · iter-159 (activity-feed-test-gap BUT-1297 + import re-review BUT-1277 — `18a8739a5`) · iter-158 (backend-rules + dart-test-gaps: BUT-1294/1295/1296/1287; BUT-1282 cancelled — `fe28279d0`/`ee3f6b487`/`565f1d330`) · iter-157 (verifier-followups BUT-1293/1289/1291/1292 — `74825b1f2`) · iter-156 (completeness-sweep widget-test gaps BUT-1274/1275/1280/1269/1270/1271 + BUT-1281) · iter-155 (cooking-mode + user-repo BUT-1283/1284/1285/1286 — `3bf7a50f3`) · iter-154 (BUT-734 + BUT-1242 — `22ab49ae9`) · iter-153 (tagging drained) · iter-152 (menu — `1711d297c`) · iter-151 (import — `673f80c87`) · iter-150..143 — se git-historiken
