# Sprint Backlog

## Sprint: hover+focus-test-safety-nets — close the two coverage gaps the iter-163 a11y/hover work left behind (2 tickets) — 2026-06-14 (iter-164)

The iter-163 sprint shipped fully in commit `83695735b` ("feat(a11y+web+settings): focus traversal, web hover affordance, auto-add-to-pantry toggle") — BUT-701 (focus traversal), BUT-710 (web hover), BUT-1306 (pantry-settings UI) all left Backlog. Working tree clean, no carry-forward.

Backlog re-read: 91 open Backlog + 0 Todo + 0 In Progress + 0 Triage. The two test-gap tickets BUT-1307/1308 were filed by that sprint as its own follow-ups; both verified against current main (the FocusTraversalGroup wrappers and HoverableCard mounts are present in production code with zero test coverage). They are the only genuinely-clean Tier-A `build` work left.

**The genuinely-clean Tier-A code slice is drained after 163 iterations.** The honest mandate split this iteration:
- **2 build picks** — BUT-1307 (widget tests for the shipped focus-traversal wrappers → Tier A build) and BUT-1308 (render tests for the shipped HoverableCard mounts → Tier A build). Both are pure test additions covering already-shipped+approved work; fully disjoint file sets (test/widget/views vs test/widget/recipe + content_cards).
- **The rest is honestly NOT auto-pick mandate** and is surfaced under "Needs you": ops/prod-blocked backfills (BUT-1169), UI-convention decisions (BUT-964), premature-optimization deferrals (BUT-1011), backend work needing external credentials (BUT-840), post-beta features, monetization scaffolding, epics, SDK-blocked deps, and true ops/launch blockers — same buckets as iter-163.

Padding to N=6 with deferred/debatable work would be false mandate — so the batch is honestly 2.

### Agent A: card-hover-coverage — render tests proving the two cards mount the hover wrapper
- [ ] **A1. Render tests asserting recipe_card + menu_card mount a HoverableCard ancestor** `[Tier A]` (BUT-1308) — new `test/widget/recipe/recipe_card_hover_test.dart` + `test/widget/common/content_cards/menu_card_hover_test.dart` (or one combined file). Pump each card widget, assert a `HoverableCard` ancestor exists in the tree, and assert the card passes design-token/square-corner decorations (no hardcoded hex / no rounded corners). Reuse the existing `test/widget/common/hoverable_card_test.dart` harness style. Do NOT re-test the wrapper's own hover/reduced-motion behavior (already covered).
  - Acceptance: A new test pumps `recipe_card.dart` and asserts a `HoverableCard` ancestor is present; another (or the same parametrized test) does the same for `menu_card.dart` · The test asserts the decoration passed is square-cornered / design-token-based (it fails if a hardcoded hex or rounded corner is introduced) · `flutter analyze` is clean on the new test file(s) · The test fails if the `HoverableCard` wrapper is removed from either call site (verified by reasoning about the assertion, not just that it passes today)
  - requiresPlanMode: false (Low pri, test-gap, single area = card-widget tests, additive test files only)

### Agent B: focus-traversal-coverage — widget tests proving the five forms keep their focus-order wrapper
- [ ] **B1. Widget test(s) asserting each of the five form views pumps a FocusTraversalGroup ancestor** `[Tier A]` (BUT-1307) — new test under `test/widget/views/` (one parametrized file preferred, e.g. `focus_traversal_group_test.dart`). For each of edit_recipe_view, skriv_sjalv_recept_view, auth/email_verification_view, settings/account_security_view, settings/notification_preferences_view: pump the view and assert a `FocusTraversalGroup` (or assigned `FocusOrder`) ancestor wraps the form fields. Topology-light per testing philosophy — assert the wrapper is present, NOT exact tab order. Use the existing view-test harness/mocks under `test/widget/views/` and `test/widget/views/settings/`.
  - Acceptance: A widget test asserts a `FocusTraversalGroup` (or `FocusOrder`) ancestor is present for at least the recipe-edit form, the auth email-verification view, and one settings form (the three that grep confirms ship the wrapper) · The test does NOT assert exact tab order (topology-light — it only proves the wrapper exists) · `flutter analyze` is clean on the new test file(s) · The test fails if the `FocusTraversalGroup` wrapper is removed from a covered view (verified by reasoning about the assertion)
  - requiresPlanMode: false (Low pri, test-gap, single area = form-view tests, additive test files only)

### Needs you (worth doing eventually — your call, not auto-pick mandate)
- **BUT-1169** (Low, backend/shopping/tech-debt) — backfill legacy meat_fish/fruit_veg shopping docs + drop legacy constants. Recommendation: **leave deferred** — the body itself defers it: dropping the constants while old docs still exist breaks rendering, and the safe path needs production telemetry + a Cloud Function backfill the loop can't run. The write-side fix already stopped the bleeding (BUT-1164). Pick up when you can run the backfill.
- **BUT-964** (Low, settings/tech-debt, child of EPIC BUT-1159) — standardise primary-action placement (FAB vs app-bar vs bottom-bar) across views. Recommendation: **needs your design-convention decision first** — it proposes a cross-view UI convention ("FAB = primary create-action everywhere; Add-friend FAB on all Friends tabs"). That's a design call across 4+ views, not a bug fix; decide the convention and it becomes buildable as a Tier B sprint.
- **BUT-840** (Low, backend/social/Improvement) — Algolia search-index freshness via on-profile-updated.ts. Recommendation: **build-review when the Algolia key path is decided** — real improvement, but extending the Cloud Function to call the Algolia admin API needs the admin credentials wired + a deploy the loop can't reach to verify end-to-end. Low impact (only stale display-name-in-search).
- **BUT-1011** (Low, backend/Improvement) — async status-polling for very large account deletion. Recommendation: **drop until telemetry warrants** — the body calls it premature optimization; the 9-minute budget covers even heavy users. File-and-defer; revisit only if production logs show `deadline-exceeded`.
- **BUT-445** (Medium, recipe/Feature) — nutrition display view. Recommendation: **leave deferred** — post-beta per the standing nutrition decision; building the view now ships an empty shell ahead of the Livsmedelsverket data source.
- **BUT-723** (Medium, Feature) — tablet master-detail layouts. Recommendation: **build-review as its own focused sprint** — a multi-day net-new responsive architecture with real visual decisions; green-light deliberately, not as an auto-pick.
- **BUT-948 / BUT-976 / BUT-1273** (Medium) — long-press semantics / per-step recipe images / URL listing-page expansion. Recommendation: **your scope/UX call** — each is net-new behavior or a convention decision, not a bug fix.
- **BUT-1268** (Medium) — cold-start split reconciliation meta-ticket. Recommendation: **your call — likely bulk-close as resolved** — the split largely landed on main (main.dart now ~268 lines, BUT-530 shipped); reconciling the child tickets is a bookkeeping judgement I shouldn't auto-execute.
- **BUT-558 / BUT-1176** (Low, tech-debt) — install DCM (commercial license) / custom_lint AST upgrade. Recommendation: **drop or defer** — tooling/licensing refinements with no active defect; the 500-line + arch guards already cover the regression class.
- **BUT-900 / BUT-1179** (Medium) — modal/dialog focus-return audit / ConflictBanner live concurrent-edit verification. Recommendation: **pair with a manual device pass** — both need a real-device screen-reader / two-client run the loop can't perform; the code fix is conditional on those findings.
- **Monetization scaffolding** (BUT-644/650/653/656/658/661/664/667/668/672/681/683/685/686 + 642 grocery-delivery) — Recommendation: **drop until a monetization decision exists** — memory records "no monetization decisions yet"; building RevenueCat/paywall/subscription scaffolding now is speculative.
- **Marketing/SEO copy** (BUT-673/680) and **EPICs** (BUT-907/1156/1157/1159/520) — Recommendation: **leave as parent trackers / your call** — epics are containers; copy is yours to write.

### Needs you (Tier D — true ops/launch/CI/secrets blockers, not worked)
- BUT-889/1167 (paid-API/Vertex credentials + AI ops remainder) · BUT-451/486/420 (staging project + CI deploy pipeline) · BUT-1229/1239/814/880/821/813/1240/1248 (deploy/console/cert-capture/PITR-drill/backfill-dispatch needing a live v2 schema change) · BUT-646/415/890/714/731/674/863/721/454/594 (store-submission/privacy-policy-hosting/entitlements/MFA-enrollment/children's-data/legal) · BUT-435/503/507/509/554/820 (Dart-SDK-blocked dependency bumps) · BUT-491 (desktop CI builds) · BUT-1149 (coverage-floor restore — gated on broad coverage burndown, not a single sprint).

### Obsolete (done in git, still open in Linear)
- (none this iteration — BUT-1299 was already archived 2026-06-14; BUT-884 already archived 2026-05-22)

### Post-Sprint Steps
- [ ] Run `dart analyze --fatal-infos`
- [ ] Run the two new test files (card-hover + focus-traversal)
- [ ] Commit, push to main
- [ ] Update Linear: BUT-1307 → Done (Tier A test-gap, test/analyze-verifiable); BUT-1308 → Done (Tier A test-gap). Leave all "Needs you" tickets untouched (flagged for Malin).

---
## ARCHIVED — iter-163 (a11y-focus + web-hover + pantry-settings-ui: BUT-701 focus traversal → Done + BUT-710 web hover → In Review + BUT-1306 pantry-settings UI → In Review — all `83695735b`; BUT-1307/1308 test-gaps filed as follow-ups) · iter-162 (mvvm-tag-routing + cooking-card-deadpath: BUT-1304/1305 → Done — `c0fb5081e`) · iter-161 (import-ssrf-parity + veckomeny-cooking-card-tests: BUT-1303/1302 → BUT-1305 filed — `130d9547c`) · iter-160 (recipe-detail-import-seam + veckomeny-decomp: BUT-1300/1301/1277 Done + BUT-1258 In Review — `8b91c165b`) · iter-159 (activity-feed-test-gap BUT-1297 + import re-review BUT-1277 — `18a8739a5`) · iter-158 (backend-rules + dart-test-gaps: BUT-1294/1295/1296/1287; BUT-1282 cancelled — `fe28279d0`/`ee3f6b487`/`565f1d330`) · iter-157 (verifier-followups BUT-1293/1289/1291/1292 — `74825b1f2`) · iter-156 (completeness-sweep widget-test gaps) · iter-155 (cooking-mode + user-repo — `3bf7a50f3`) · iter-154 (BUT-734 + BUT-1242 — `22ab49ae9`) · iter-153..143 — se git-historiken
