# Sprint Backlog

## Sprint: recipe-detail-import-seam + veckomeny-decomp — back an untested re-extract path with a DI seam, formally re-run the import-flow specialist reviews, and bring veckomeny_view under the file-size limit (3 tickets) — 2026-06-14 (iter-160)

The iter-159 sprint shipped fully (BUT-1297 activity-event repo test = `18a8739a5`; BUT-1277 parked In Review and spawned BUT-1300/1301). Working tree clean, no carry-forward.

The genuinely-clean Tier-A code slice stays thin — of 98 open Backlog tickets, the overwhelming majority are launch-gated/ops (BUT-415/420/451/486/492/646/814/819/821/880/1166/1229/1239...), monetization/marketing scaffolding (BUT-650/658/661/664/668/672/683/685/686/916/...), epics (BUT-907/1156/1157/1159/520), SDK-blocked dependency bumps (BUT-435/503/507/509/554/1218), or product/UX decisions that are Malin's call (BUT-976/941/945/1261/1276/1290/...). Three honest, mostly-disjoint Tier-A/C items remain that I can vouch for this iteration.

### Agent A: import-flow seam + re-review — give the re-extract path a test seam, then run the deferred specialist reviews against the final import diff
- [ ] **A1. Add a DI seam to `RecipeDetailViewModel.reextractFromSource` + unit-test the contract** `[Tier A]` (BUT-1300) — `lib/viewmodels/recipe_detail_viewmodel.dart` (~line 525: inject an `ImportStrategy Function(SourceArtefactType)` factory defaulted to the current `UrlImportStrategy()`/`TextImportStrategy()` construction), new `test/unit/viewmodels/recipe_detail_viewmodel_reextract_test.dart`. Production happy-path behavior must not change — seam is purely additive (default arg = today's behavior).
  - Acceptance: A constructor/field seam exists that lets a test supply a fake `ImportStrategy` without hitting network · Strategy-selection test passes: `SourceArtefactType.url` → url strategy, every other type → text strategy · Success test passes: `id` + `createdAt` unchanged and the original `sourceArtefact` is re-attached, while title/ingredients/instructions/etc. are overwritten from the re-extraction · All three failure paths (import throws · result not-success/null recipe · `updateRecipe` false/throws) return `ReextractOutcome.failure` and leave in-memory `_recipe` unmutated · No production happy-path behavior change (default factory reproduces current concrete construction)
- [ ] **B1. Re-dispatch code-reviewer + testing-specialist against the final import-flow files** `[Tier A]` (BUT-1301) — review-only over `lib/viewmodels/url_import_viewmodel.dart`, `lib/viewmodels/smart_import_viewmodel.dart`, `lib/views/recipe_detail_view.dart`, `lib/viewmodels/recipe_detail_viewmodel.dart`, `lib/models/tagging/recipe_personal_tag.dart` at current committed state (scope hint `git diff 7a3a5ab14..10325a5bb -- <files>`). Fix Critical/High inline; file the rest as follow-ups; then BUT-1277 → Done. Note: A1 will modify `recipe_detail_viewmodel.dart`, so run B1's review AFTER A1's seam lands so the reviewers see final state (same batch, sequenced).
  - Acceptance: `code-reviewer` verdict recorded against the five files at their post-A1 state, captured in the BUT-1277 close-out comment · `testing-specialist` verdict recorded against the same five files in the same comment · Every Critical/High finding is fixed inline (in the diff) or filed as a new Linear ticket referenced in the close-out · If no findings, the close-out explicitly states "clean — no missed findings against final state"; BUT-1277 moved to Done only after both verdicts captured

### Agent B: veckomeny-decomp — bring the weekly-menu view back under the 500-line limit
- [ ] **C1. Extract veckomeny_view selection-bar / overflow-menu / move-picker into a sibling widget** `[Tier C]` (BUT-1258) — `lib/views/veckomeny_view.dart` (currently 606 lines, NOT the 832 the ticket claims — line count is stale, but it still exceeds the 500-line limit so the intent holds), extract the selection action bar + copy-week overflow menu + day/slot move-picker bottom sheet into a new sibling under `lib/widgets/menu/` (e.g. `veckomeny_selection_widgets.dart`), alongside the existing `lib/widgets/menu/veckomeny_dialogs.dart`. Behavior must not change — pure structural move.
  - Acceptance: `lib/views/veckomeny_view.dart` is ≤500 lines after extraction (or, if extraction is judged disproportionate, an `ACCEPTED_LARGE_FILES.md` entry is added with rationale at the file's true current line count) · A new sibling widget file under `lib/widgets/menu/` holds the extracted selection-bar / overflow-menu / move-picker UI and is itself ≤500 lines · No behavioral change: the multi-select move, copy-week, and move-picker flows work identically (existing veckomeny tests stay green) · `flutter analyze` clean on both files

### Needs you (not built — flagged for your call)
- **BUT-884** (Low) — PREMISE STALE, recommend **reframe or drop**. Claims "~20 CircularProgressIndicator sites + add `LoadingIndicator.determinate()` if missing." Re-measured: only 14 raw `CircularProgressIndicator` refs in all of lib/, and most are the indicator infrastructure itself (`loading_indicator.dart`, `adaptive_activity_indicator.dart`, `progress_overlay.dart`, `pea_loading_animation.dart` legitimately implement it). The determinate `value:` constructor already shipped (BUT-1173). What's left is a handful of view-level overlay sites and a judgment call about which count as "image/upload overlays" vs canonical infra — small and debatable, not the bulk migration the ticket implies.
- **BUT-1169** (Low) — your call, recommend **leave deferred**. Drop-legacy-`meat_fish`/`fruit_veg`-constants slice. The constants can't be removed while old Firestore docs still carry the aggregate keys (rendering would break), and the backfill needs production telemetry + a Cloud Function this loop can't reach (Tier D). The write-side fix already stopped new aggregate keys; this only matters once a prod backfill runs.
- **BUT-1276** (Low) — carried, your call. BUT-1205 overwrite-confirm is unconditional, not edit-aware. Recommendation: **accept-and-close** — always-confirming is the safe choice under the BUT-954 destructive-action convention; building `hasUserEdits` dirty-tracking is disproportionate. Behavior/UX call, so it parks.
- **BUT-1261** (Medium) — carried, your call. Conflict diff view uses one generic renderer and stringifies ingredient lists rather than semantic add/remove/reorder. Recommendation: lean **(b) narrow the acceptance** to the shipped generic diff once you confirm it reads clearly — per-type semantic diffing is a sizeable build for a feature not yet requested.
- **BUT-1290** (Medium) — carried, your call. Fate of the one-time activity-feed hint banner (backend mechanism + ARB string exist; only the visible `privacy_section.dart` banner is missing). Recommendation: lean **won't-build** — the in-feed hint already nudges once; a settings-page banner is redundant.
- **BUT-1288** (Low) — carried. iOS Info.plist/AppDelegate doc-confirm is trivial, but the substantive half (on-device test that a real timer notification fires) needs a Mac the loop can't reach (Tier D). Do the doc note only if you want it tracked.
- **BUT-1260** (Medium) — carried, recommend **reframe**. Asks for a Phase-A/Phase-B cold-start ordering seam, but `runPhaseA`/`runPhaseB` don't exist — the BUT-431 cold-start split was never shipped. No ordering to test. Re-file against the real (un-done) BUT-431, or drop.
- **BUT-840** (Low) — carried, recommend **reframe / drop**. Asks to extend `on-profile-updated.ts` to update an Algolia mirror, but there is NO Algolia integration anywhere — indexing is client-side. This is a new external-API surface (Algolia admin key as secret + deploy = effectively Tier D), not the "extend an existing CF" the ticket implies.

### Obsolete (done in git, still open in Linear)
- (none — iter-159's BUT-1297 already Done via `18a8739a5`; BUT-1277 correctly parked In Review with BUT-1300/1301 carrying its remainder)

### Post-Sprint Steps
- [ ] Run `dart analyze --fatal-infos`
- [ ] Run `test/unit/viewmodels/recipe_detail_viewmodel_reextract_test.dart` + existing veckomeny tests
- [ ] Commit, push to main
- [ ] Update Linear: BUT-1300 → Done (Tier A, fully verifiable); BUT-1301 → Done once both specialist verdicts captured (and BUT-1277 → Done with it); BUT-1258 → In Review (Tier C structural refactor — your eyes on the extraction) unless fully test-proven mechanical. Leave BUT-884/1169/1276/1261/1290/1288/1260/840 untouched (flagged for Malin).

---
## ARCHIVED — iter-159 (activity-feed-test-gap BUT-1297 + import-flow re-review BUT-1277 → spawned BUT-1300/1301 — `18a8739a5`) · iter-158 (backend-rules + dart-test-gaps: BUT-1294 activity_events rules + BUT-1295/1296 + BUT-1287 GDPR-audit; BUT-1282 cancelled — `fe28279d0`/`ee3f6b487`/`565f1d330`) · iter-157 (verifier-followups BUT-1293/1289/1291/1292 — `74825b1f2`) · iter-156 (completeness-sweep widget-test gaps BUT-1274/1275/1280/1269/1270/1271 + security re-review BUT-1281) · iter-155 (cooking-mode + user-repo BUT-1283/1284/1285/1286 — `3bf7a50f3`) · iter-154 (BUT-734 user-repo split + BUT-1242 multi-timer cooking mode — `22ab49ae9`) · iter-153 (tagging drained) · iter-152 (menu BUT-1278/1279/1043/930 — `1711d297c`) · iter-151 (import BUT-1040/931/947/903/1205 — `673f80c87`) · iter-150..143 — se git-historiken
