# Sprint Backlog

## Sprint: a11y+hover-test-completeness — close the two coverage-symmetry gaps the iter-164 tests left behind (2 tickets) — 2026-06-15 (iter-165)

Backlog re-read: 92 open Backlog + 0 Todo + 0 In Progress + 0 Triage. Working tree had only two untracked scratch files (tmp_linear_chunk*.txt), no carry-forward from iter-164.

The iter-164 sprint shipped its two test files in commit `79c2cc98f` ("test(widgets): hover-affordance + reading-order traversal guards (BUT-1308, BUT-1307)") and filed three completeness-sweep follow-ups of its own: BUT-1309, BUT-1310, BUT-1311. Two of those three are genuinely-clean Tier-A `build` work (additive test assertions against already-shipped, already-approved a11y/hover code); the third is a decision/bookkeeping ticket that is Malin's call.

**The genuinely-clean Tier-A code slice remains drained after 164 iterations** — exactly as iter-163/164 reported. The only new clean work is the self-filed test-symmetry follow-ups. Padding to N=6 with deferred/debatable work would be false mandate, so the honest batch is 2.

- **2 build picks** — BUT-1311 (add the missing positive-case `enabled == true` assertion to the two card hover-test files → Tier A build) and BUT-1309 (extend the focus-traversal test to the recipe-edit + email-verification views the iter-164 test silently skipped → Tier A build). Fully disjoint file sets (card tests vs form-view tests).
- **The rest is honestly NOT auto-pick mandate** — surfaced under "Needs you" (same buckets as iter-163/164: ops/prod-blocked backfills, UI-convention decisions, post-beta features, monetization scaffolding, epics, SDK-blocked deps, true ops/launch blockers) plus BUT-1310 (a "decide & record" acceptance-reconciliation ticket — already effectively resolved in code comments + the testing-specialist knowledge file; recommend bulk-close, but it's a bookkeeping judgement, not auto-build).

### Agent A: card-hover-positive-case — symmetric positive assertion on the two card hover tests
- [ ] **A1. Add a positive-case `enabled == true` assertion to each card hover test** `[Tier A]` (BUT-1311) — edit existing `test/widget/recipe/recipe_card_hover_test.dart` + `test/widget/common/content_cards/menu_card_hover_test.dart`. Each file already asserts `hoverable.enabled == false` when no `onTap` is passed; add the mirror: pump the card WITH an `onTap` and assert the mounted `HoverableCard.enabled` is `true`. This closes the `enabled: onTap != null` wiring so a refactor that hard-codes `enabled: false` (killing hover while a tap handler exists) would fail. Do NOT change production code; do NOT re-test the wrapper's own hover/reduced-motion behavior (already covered).
  - Acceptance: `recipe_card_hover_test.dart` gains a test that pumps `RecipeCard` with an `onTap` and asserts the descendant `HoverableCard.enabled` is `true` · `menu_card_hover_test.dart` gains the equivalent positive assertion for `MenuCard` with an `onTap` · No production file under `lib/` is modified (test-only diff) · `flutter analyze` is clean on both edited test files and both files pass
  - requiresPlanMode: false (Low pri, test-gap, single area = card-widget tests, additive assertions to existing files)

### Agent B: focus-traversal-view-coverage — extend the reading-order test to the two named-but-skipped views
- [ ] **B1. Extend the focus-traversal test to edit_recipe_view + email_verification_view** `[Tier A]` (BUT-1309) — edit existing `test/widget/views/focus_traversal_group_test.dart`. It currently covers ONLY account_security_view; BUT-1307's acceptance named three required surfaces. Add cases that boot `lib/views/edit_recipe_view.dart` (NOTE: real path is `lib/views/edit_recipe_view.dart`, not `lib/views/recipe/` as the ticket body assumes — Step-0 path correction) and `lib/views/auth/email_verification_view.dart`, focus the top field, press Tab, and assert the form fields receive focus strictly top-to-bottom (reading order), reusing the existing test's pattern. Topology-light per testing philosophy and per the test's own documented caveat: assert the reading-order CONTRACT (Tab walks fields top-to-bottom), NOT the bare presence of the wrapper widget (the file header explains the wrapper is a no-op for observable order in a plain Column). Use the existing view-test harness/mocks (`BaseUnitTest`, `TestServiceLocator`, `createLocalizedTestApp`); register whatever services each view resolves on construction (mirror the account_security setUp).
  - Acceptance: `focus_traversal_group_test.dart` gains a case that boots `email_verification_view.dart`, focuses the top field, sends Tab, and asserts the text fields are visited in strictly top-to-bottom (reading) order · It gains an equivalent case for `edit_recipe_view.dart` (or `skriv_sjalv_recept_view.dart` if edit_recipe needs heavier mocking) · The new cases assert reading-order traversal, NOT `source.contains('FocusTraversalGroup')` and NOT exact wrapper presence (honors the rejected-source-grep caveat in the file header) · No production file under `lib/` is modified (test-only diff) · `flutter analyze` is clean and the file passes
  - requiresPlanMode: false (Low pri, test-gap, single area = form-view tests, additive cases to one existing file)

### Needs you (worth doing eventually — your call, not auto-pick mandate)
- **BUT-1310** (Low, no labels) — "decide & record": BUT-1307's literal acceptance bullet ("test fails if the FocusTraversalGroup wrapper is removed") is un-meetable — in a plain Column the wrapper is a no-op for observable tab order, so no black-box test can catch its removal. Recommendation: **bulk-close as already-resolved** — the caveat is already documented in the test file header AND the testing-specialist knowledge file; the test correctly guards the reading-order contract instead. The only remaining action is amending BUT-1307's acceptance wording, which is a bookkeeping/decision call, not code.
- **BUT-1067** (Low, Improvement) — backfill `description` metadata for ~3000 ARB i18n keys. Recommendation: **leave asleep** — the ticket body itself says "YAGNI until a third locale or external translator is engaged"; pure translator-context backfill with no UX/code change.
- **BUT-1169** (Low, backend/shopping/tech-debt) — backfill legacy meat_fish/fruit_veg shopping docs + drop legacy constants. Recommendation: **leave deferred** — needs a production backfill Cloud Function the loop can't run; the write-side fix (BUT-1164) already stopped the bleeding.
- **BUT-964** (Low, settings/tech-debt) — standardise primary-action placement (FAB vs app-bar vs bottom-bar). Recommendation: **needs your design-convention decision first** — a cross-view UI convention call, not a bug fix.
- **BUT-840** (Low, backend/social/Improvement) — Algolia search-index freshness via on-profile-updated.ts. Recommendation: **build-review once the Algolia admin-key path is decided** — needs credentials + a deploy the loop can't verify end-to-end. Low impact (stale display-name-in-search only).
- **BUT-1011** (Low, backend/Improvement) — async status-polling for very large account deletion. Recommendation: **drop until telemetry warrants** — body calls it premature optimization.
- **BUT-445 / BUT-643** (Medium/Low, recipe) — nutrition display view / Livsmedelsverket integration. Recommendation: **leave deferred** — post-beta per the standing nutrition decision.
- **BUT-723 / BUT-711 / BUT-706** (Med/Low) — tablet master-detail / foldable hinge handling / iOS CupertinoNavigationBar. Recommendation: **build-review as their own focused responsive/platform sprints** — net-new layout architecture with real visual decisions, not auto-pick.
- **BUT-916 / BUT-920** (Med, account) — social sign-in (OAuth) / guest demo mode. Recommendation: **your scope call** — memory records social login is post-beta; demo mode is a product decision.
- **BUT-610** (Med, recipe/backend) — audit + harden offline mode. Recommendation: **build-review as a scoped sprint** — an audit-then-harden task spanning many surfaces; needs a defined scope before auto-building.
- **BUT-431 / BUT-1268** (Med) — cold-start reduction / cold-start split reconciliation meta-ticket. Recommendation: **your call — likely bulk-close BUT-1268 as resolved** (BUT-530 landed, main.dart now ~268 lines); BUT-431 is a perf task needing a profiling pass first.
- **BUT-900 / BUT-1179** (Med) — modal/dialog focus-return audit / ConflictBanner live concurrent-edit verification. Recommendation: **pair with a manual device pass** — both need a real-device screen-reader / two-client run the loop can't perform.
- **BUT-948 / BUT-976 / BUT-1273 / BUT-941 / BUT-934 / BUT-945 / BUT-684 / BUT-950** — long-press semantics / per-step images / URL listing-page expansion / OS multi-share / re-engagement notifs / easier rejoin / handwritten-OCR / deletion grace period. Recommendation: **your scope/UX call** — each is net-new behavior or a convention decision, not a bug fix.
- **BUT-496 / BUT-520 / BUT-558 / BUT-1176 / BUT-554** (tech-debt) — DORA metrics / ChangeNotifier→BaseViewModel epic migration / DCM install / custom_lint AST upgrade / build_runner migration plan. Recommendation: **defer** — no active defect; the 500-line + arch guards already cover the regression class. BUT-520 is a 62-VM epic best run as its own deliberate sprint.
- **Monetization scaffolding** (BUT-644/650/653/656/658/661/664/667/668/672/681/683/685/686 + 642 grocery-delivery) — Recommendation: **drop until a monetization decision exists** — memory records "no monetization decisions yet".
- **Marketing/SEO copy** (BUT-673/680) and **EPICs** (BUT-907/1156/1157/1159/520) — Recommendation: **leave as parent trackers / your call** — epics are containers; copy is yours to write.

### Needs you (Tier D — true ops/launch/CI/secrets blockers, not worked)
- BUT-1229 (deploy + cook-snap backfill before rules/indexes deploy) · BUT-451/486/420 (staging project + CI deploy pipeline) · BUT-813/814/821/880/1239/1248 (GCP alert policies / cert-pin SHA-256 capture / monitoring alert / PITR drill / model-hash CI guard / schemaVersion migration-dispatch — all need live console/deploy) · BUT-889/1167/1240/818 (paid-API golden corpora / AI-ops remainder / NER device-runner corpus / Vision SafeSearch — credentials + device runners) · BUT-646/415/890/714/731/674/863/721/454/594/916 (store-submission / privacy-policy hosting / entitlements / MFA-enrollment / children's-data / legal) · BUT-435/503/507/509/554/820/1218 (Dart-SDK-blocked or major dependency bumps) · BUT-491 (desktop CI builds) · BUT-1149 (coverage-floor restore — gated on broad coverage burndown) · BUT-1288/1299 (on-device smoke test / close-out confirm needing a device).

### Obsolete (done in git, still open in Linear)
- (none this iteration)

### Post-Sprint Steps
- [ ] Run `dart analyze --fatal-infos`
- [ ] Run the two edited test files (card-hover + focus-traversal)
- [ ] Commit, push to main
- [ ] Update Linear: BUT-1311 → Done (Tier A test-gap, test/analyze-verifiable); BUT-1309 → Done (Tier A test-gap). Leave all "Needs you" tickets untouched (flagged for Malin).

---
## ARCHIVED — iter-164 (hover+focus-test-safety-nets: BUT-1308 card-hover render tests + BUT-1307 focus-traversal test → Done — `79c2cc98f`; BUT-1309/1310/1311 filed as follow-ups) · iter-163 (a11y-focus + web-hover + pantry-settings-ui: BUT-701 → Done, BUT-710 + BUT-1306 → In Review — `83695735b`) · iter-162 (mvvm-tag-routing + cooking-card-deadpath: BUT-1304/1305 → Done — `c0fb5081e`) · iter-161 (import-ssrf-parity + veckomeny-cooking-card-tests — `130d9547c`) · iter-160 (recipe-detail-import-seam + veckomeny-decomp — `8b91c165b`) · iter-159 (activity-feed-test-gap — `18a8739a5`) · iter-158 (backend-rules + dart-test-gaps — `fe28279d0`) · iter-157 (verifier-followups — `74825b1f2`) · iter-156..143 — se git-historiken
