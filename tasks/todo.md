# Sprint Backlog

## Sprint: a11y-focus + web-hover + pantry-settings-ui — the three honest build/build-review picks the drained backlog can still vouch for (3 tickets) — 2026-06-14 (iter-163)

The iter-162 sprint shipped fully (BUT-1304 MVVM tag-routing → Done; BUT-1305 cooking-card dead-path → Done, both in commit `c0fb5081e`). Working tree clean, no carry-forward.

Backlog re-read: 92 open Backlog + 0 Todo + 0 In Progress + 0 Triage (the 4 ex-Todo items — BUT-530/1149/431/1050 — have all moved: BUT-530 shipped `d057b7e3a`, BUT-1050 shipped `5aede8841`, BUT-1149 reverted to Backlog, BUT-431 superseded by the cold-start work). No obsolete tickets (git mentions of BUT-431/1149/1268 are cross-references in other commits, not completions).

The genuinely-clean Tier-A code slice is essentially drained after 162 iterations. The honest mandate split this iteration:
- **3 build/build-review picks** — BUT-701 (a11y focus traversal, functional/no-design-choice → build), BUT-710 (web hover affordances, visual → build-review), BUT-1306 (pantry-settings UI, follow-up to already-shipped+approved BUT-1050 backend → build-review). All three touch fully-disjoint file sets.
- **The rest is honestly NOT auto-pick mandate** and is surfaced under "Needs you": post-beta features (BUT-445 nutrition, BUT-723 tablet master-detail), product/UX-convention decisions (BUT-948 long-press, BUT-976 per-step images, BUT-1273 listing-page import), tooling/licensing decisions (BUT-558 DCM, BUT-1176 custom_lint), the cold-start reconciliation meta-ticket (BUT-1268), manual-QA/screen-reader passes (BUT-900), monetization scaffolding (no monetization decisions per memory), epics, SDK-blocked deps, and true ops/launch blockers.

Padding to N=6 with deferred/debatable work would be false mandate — so the batch is honestly 3.

### Agent A: a11y-focus-traversal — extend FocusTraversalGroup to the remaining form-heavy views
- [ ] **A1. Wrap remaining forms/grouped-controls with FocusTraversalGroup + explicit FocusOrder where visual order diverges** `[Tier A]` (BUT-701) — `lib/views/edit_recipe_view.dart`, `lib/views/skriv_sjalv_recept_view.dart`, `lib/views/auth/email_verification_view.dart`, `lib/views/settings/account_security_view.dart`, `lib/views/settings/notification_preferences_view.dart`. Follow the existing in-repo pattern in `lib/widgets/common/input/shopping_item_dialog.dart` and `lib/widgets/common/navigation/adaptive_navigation.dart`. Tab-order only — no visual change.
  - Acceptance: At least the recipe-edit form, the auth email-verification view, and one settings form are wrapped in a `FocusTraversalGroup` (or assigned `FocusOrder`) where they previously had none · No visual/layout change is introduced (the diff adds focus-ordering wrappers only, no color/spacing/widget-structure edits beyond the wrapping) · `flutter analyze` is clean on every changed file · The 5 files that already use focus ordering are not regressed (no existing `FocusTraversalGroup`/`FocusOrder` removed)
  - requiresPlanMode: false (Medium pri, Improvement, single area = a11y/focus, no security/cross-module surface; in-repo pattern to copy)

### Agent B: web-hover-affordances — add hover feedback to custom interactive widgets
- [ ] **B1. Add a HoverableCard wrapper (MouseRegion + AnimatedContainer) and apply to custom interactive widgets** `[Tier B]` (BUT-710) — new `lib/widgets/common/hoverable_card.dart` (or a mixin), applied to `lib/widgets/recipe/recipe_card.dart` and a representative chip/badge/social-card. Honor reduced-motion (`MediaQuery.disableAnimations`). Subtle elevation or bg-color shift only, using design-system tokens (no rounded corners — square design language per memory).
  - Acceptance: A reusable hover wrapper exists and is applied to at least `recipe_card.dart` plus one more custom interactive widget · Hover effect uses design-system color/elevation tokens (no hardcoded hex; square corners preserved) · Reduced-motion preference is honored (animation is skipped/instant when `MediaQuery.disableAnimations` is true) · `flutter analyze` clean on changed files · No behavior change on tap/long-press — only an added hover (pointer-enter/exit) affordance
  - requiresPlanMode: false (Medium pri, Improvement, single area = widgets/web-ui, additive wrapper, no cross-module behavior change)
  - signoffReason: How the hover feels visually — elevation lift vs background-color shift, and how subtle — is Malin's design call; build a best guess and park in In Review with the preview.

### Agent C: pantry-settings-ui — surface the already-shipped auto-add-to-pantry preference
- [ ] **C1. Settings toggle + first-checkoff prompt + live wiring for autoAddBoughtToPantry** `[Tier B]` (BUT-1306) — settings toggle row in `lib/views/settings/settings_hub_view.dart` (or a shopping/pantry settings sub-view) bound to `UserService.setAutoAddToPantry` reading `currentUserProfile.autoAddBoughtToPantry`; first-checkoff one-time prompt gated on `pantryAutoAddPrompted == false` then `markPantryAutoAddPrompted()`; live wiring from the shopping checkoff path (`lib/viewmodels/unified_shopping_viewmodel.dart` → `lib/services/shopping/shopping_checkoff_pantry_service.dart` `onItemCheckedOff`). Backend (BUT-1050, commit `5aede8841`) is already shipped + tested. Do NOT push service/userId deps into the low-level `toggleItemBought` mutation.
  - Acceptance: A settings toggle reads and persists `autoAddBoughtToPantry` and survives app restart (bound to `UserService.setAutoAddToPantry`, not a local-only bool) · The first-checkoff prompt fires exactly once — gated on `pantryAutoAddPrompted == false` and calls `markPantryAutoAddPrompted()` after showing, so it never re-nags · With the toggle ON, checking off an item routes through `ShoppingCheckoffPantryService.onItemCheckedOff` with the real userId + prior bought-state; with it OFF nothing is added · The low-level `ShoppingItemManagementModule.toggleItemBought` gains NO service/userId dependency (wiring passes context from the ViewModel layer)
  - requiresPlanMode: false — BUT it is Tier B with multi-file wiring; if Step-0 reveals the checkoff seam needs threading userId through 3+ layers, expand the file-plan inline per Phase 1.5 before editing.
  - signoffReason: Prompt copy + placement and the settings-row wording/location are UX choices needing Malin's eyes — build per design-system + memory prefs, park in In Review with a screenshot.

### Needs you (worth doing eventually — your call, not auto-pick mandate)
- **BUT-445** (Medium, recipe/Feature) — nutrition display view. Recommendation: **leave deferred** — the ticket body itself says "post-beta, per the standing nutrition decision" with Livsmedelsverket API auto-population. Building the view now ships an empty shell ahead of the data source. Drop until the post-beta nutrition push.
- **BUT-723** (Medium, Feature) — tablet master-detail layouts. Recommendation: **build-review when ready** — a 3-5 day net-new responsive architecture (MasterDetailScaffold across 4 view families). Real product investment + visual decisions; green-light it as its own focused sprint, not an auto-pick.
- **BUT-948** (Medium, tech-debt) — define long-press semantics. Recommendation: **needs your decision first** — the ticket asks you to pick Option A (long-press = multi-select) vs Option B (long-press = context menu); it's a UX convention change across 5 views. Decide the convention, then it becomes buildable.
- **BUT-976** (Medium, recipe/idea) / **BUT-1273** (Medium, import) — per-step recipe images / URL listing-page link expansion. Recommendation: **build-review / your scope call** — both are net-new behavior (new image-upload surface; detect+scrape a listing page), not bug fixes. Worth doing but a feature green-light, not an auto-pick.
- **BUT-1268** (Medium) — cold-start split reconciliation meta-ticket. Recommendation: **your call — likely close most as resolved**. Step-0 against current main: the split largely landed (`lib/app/butlery_app.dart` exists, `main.dart` is now 268 lines = under the 500 limit, BUT-530 shipped `d057b7e3a`), `ACCEPTED_LARGE_FILES.md` DOES now exist on main (so BUT-1259's "file absent" premise is stale), and `app_initializer.dart` was never created. Reconciling/closing the 4 child tickets (BUT-1258/1259/1260/530) is a bookkeeping judgement call I shouldn't auto-execute — recommend you skim and bulk-close.
- **BUT-558** (Low, tech-debt) — install DCM. Recommendation: **drop or reframe** — DCM is now a commercial licensed CLI (cost for a solo dev); the ticket asks you to evaluate license-vs-free-alternative. No active defect (the 500-line limit is already enforced separately). Tooling/licensing decision, low value.
- **BUT-1176** (Low, tech-debt) — custom_lint/AST upgrade. Recommendation: **leave deferred** — own body says pick up only if custom_lint is being added for other reasons; the arch-test guard already prevents the common regression and BUT-1001 found zero current leaks. Adds a dependency for a refinement.
- **BUT-900** (Medium, idea) — modal/dialog focus-return audit. Recommendation: **partially ops-blocked** — the core ("does focus return to the trigger?") needs a manual TalkBack/VoiceOver pass that the loop can't run; the code fix is conditional on that audit's findings. Pair it with a real device pass.
- **Monetization scaffolding** (BUT-644/650/658/661/664/668/672/683/685/686/916/681/667 + 642 grocery-delivery) — Recommendation: **drop until a monetization decision exists** — memory records "no monetization decisions yet". Building RevenueCat/paywall/subscription scaffolding now is speculative.
- **Marketing/SEO copy** (BUT-673/680) and **EPICs** (BUT-907/1156/1157/1159/520) — Recommendation: **leave as parent trackers / your call** — epics are containers; copy is yours to write.

### Needs you (Tier D — true ops/launch/CI/secrets blockers, not worked)
- BUT-889 (CI Vertex AI credentials + paid-API plumbing) · BUT-451/486/420 (staging project + CI deploy pipeline) · BUT-1229/814/880/819/821/813/1239/1240 (deploy/console/cert-capture/PITR-drill/CI-lane) · BUT-646/415/890/714/731/594/674/863/721 (store-submission/privacy-policy-hosting/entitlements/MFA-enrollment/legal) · BUT-435/503/507/509/554/820/1218 (Dart-SDK-blocked dependency bumps) · BUT-491 (desktop CI builds).

### Obsolete (done in git, still open in Linear)
- (none this iteration)

### Post-Sprint Steps
- [ ] Run `dart analyze --fatal-infos`
- [ ] Run relevant tests (focus-traversal widget smoke; pantry-settings VM/widget; hover widget render)
- [ ] Commit, push to main
- [ ] Update Linear: BUT-701 → Done (Tier A, functional a11y, test/analyze-verifiable); BUT-710 → In Review + screenshot (Tier B); BUT-1306 → In Review + screenshot (Tier B). Leave all "Needs you" tickets untouched (flagged for Malin).

---
## ARCHIVED — iter-162 (mvvm-tag-routing + cooking-card-deadpath: BUT-1304 editTags→VM routing → Done + BUT-1305 cooking-card dead-path → Done — `c0fb5081e`) · iter-161 (import-ssrf-parity + veckomeny-cooking-card-tests: BUT-1303/1302 → BUT-1305 filed — `130d9547c`) · iter-160 (recipe-detail-import-seam + veckomeny-decomp: BUT-1300/1301/1277 Done + BUT-1258 In Review — `8b91c165b`) · iter-159 (activity-feed-test-gap BUT-1297 + import re-review BUT-1277 — `18a8739a5`) · iter-158 (backend-rules + dart-test-gaps: BUT-1294/1295/1296/1287; BUT-1282 cancelled — `fe28279d0`/`ee3f6b487`/`565f1d330`) · iter-157 (verifier-followups BUT-1293/1289/1291/1292 — `74825b1f2`) · iter-156 (completeness-sweep widget-test gaps) · iter-155 (cooking-mode + user-repo — `3bf7a50f3`) · iter-154 (BUT-734 + BUT-1242 — `22ab49ae9`) · iter-153..143 — se git-historiken
