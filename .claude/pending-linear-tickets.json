{
  "pendingSince": "2026-03-07",
  "batches": [
    {
      "scanFocus": "ultrathink-deep-scan-16-theming-concurrency-tests-widgets-accessibility-contracts",
      "tickets": [
        {
          "findingKey": "missing-async-init-guards-double-init",
          "title": "Fix missing async init guards causing double-init races in 3 services",
          "labels": [
            "Bug",
            "backend"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. UnifiedRecipeService.initialize() â€” no _isInitializing guard\n`lib/services/unified/unified_recipe_service.dart:442â€“530`\n\nOnly checks `_isInitialized` (completed state), not in-progress. Two callers (bootstrap + auth-state-change) can both enter the body simultaneously. Both call `_recipes.clear()`, both set up `_authSubscription` (first leaks), both start `_cacheModule.startFirebaseSync()`. Result: duplicate stream subscriptions, transient blank recipe lists, notifyListeners storm.\n\nCorrect pattern exists at `SharedContentCoordinatorViewModel:209`: `if (_isInitialized || _isInitializing) return;`\n\n### 2. FriendsStateManager.initialize() â€” same missing guard\n`lib/services/unified/friends/friends_state_manager.dart:63â€“97`\n\nBoth FriendsViewModel constructor and re-auth events can call initialize() simultaneously. Both pass `_isInitialized == false`, both call `Future.wait([_loadFriends...])`, both call `_setupRealtimeListeners()` which cancels and re-creates subscriptions â€” but the first call's subscriptions get cancelled before delivering data.\n\n### 3. FriendsProfileCacheManager.loadUserProfilesForRequests() â€” duplicate requests\n`lib/viewmodels/friends/friends_profile_cache_manager.dart:27â€“82`\n\n`_isLoadingUserProfiles` is a UI flag, not a re-entrancy guard. Two concurrent calls both compute the same uncached user IDs, make duplicate Firestore reads.\n\n## Why It Matters\nDouble-init causes leaked stream subscriptions, duplicate Firestore listeners, transient blank states on app start.\n\n## Suggested Fix\n1-2. Add `bool _isInitializing = false` guard matching SharedContentCoordinatorViewModel pattern\n3. Add `if (_isLoadingUserProfiles) return` re-entrancy guard"
        },
        {
          "findingKey": "stale-closures-uncancellable-retries-data-loss",
          "title": "Fix stale closures and uncancellable retries causing silent data loss (5 locations)",
          "labels": [
            "Bug",
            "backend"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. PersonalRecipeModule sync retry captures stale recipe â€” DATA LOSS\n`lib/services/unified/modules/personal_recipe_module.dart:724â€“807`\n\nRetry closure captures the `recipe` object from the original failed call, not the current version in `_recipes`. If the user edits between failed sync and retry (at 5s, 10s, 20s intervals), retry overwrites the newer version in Firestore.\n\n### 2. SocialOperationsInitializer.scheduleRetry() â€” unbounded recursive retry\n`lib/services/unified/helpers/social_operations_initializer.dart:98â€“113`\n\nNo max retry count, no cancellation token. `delay.inSeconds * 2` with sub-1s initial delay produces tight loop.\n\n### 3. RetaggingScheduler â€” _isRetagging is not an async lock\n`lib/services/tagging/retagging_scheduler.dart:57â€“95`\n\nPeriodic timer + scheduled call both enter `_performRetagging()`, causing duplicate `_saveRecipe()`. Also `Future.delayed(5s)` survives dispose().\n\n### 4. FriendsViewModel stale closure in Future.delayed\n`lib/viewmodels/friends_viewmodel.dart:399â€“408`\n\nCallback fires between `_isDisposed = true` and `super.dispose()` â€” assertion errors during logout.\n\n## Why It Matters\nFinding 1 causes silent data loss. Findings 2-3 cause unbounded retries or duplicate writes.\n\n## Suggested Fix\n1. Re-read recipe from `_recipes[recipe.id]` at retry time\n2. Add max retry count, store Timer reference for cancellation\n3. Use Completer-based lock; cancel Future.delayed in dispose\n4. Check _isDisposed after super.dispose()"
        },
        {
          "findingKey": "cache-drift-presence-deadlock-search-mixing",
          "title": "Fix cache counter drift, presence stream deadlock, and search term mixing (3 concurrency bugs)",
          "labels": [
            "Bug",
            "social",
            "recipe"
          ],
          "priority": 2,
          "description": "## Finding\n\n### 1. IntelligentCacheManager â€” memory counter drift â†’ unbounded growth\n`lib/services/performance/intelligent_cache_manager.dart:257â€“294, 460â€“471`\n\nTwo Timer.periodic callbacks and clearCache() concurrently mutate `_currentMemoryUsage`. clearCache() sets counter to 0 while prefetch is mid-await. After await resumes, `_cacheRecipe` adds `+= size` on zeroed counter. Counter drifts, `_ensureMemoryAvailable()` never evicts â†’ OOM.\n\n### 2. PresenceService â€” asyncMap + inner await for deadlocks\n`lib/services/presence_service.dart:391â€“409`\n\n`asyncMap` on broadcast stream with inner `await for (... in streams[i].take(1))`. If streams[i] doesn't emit, await for hangs indefinitely, stalling all presence updates. 3+ participant scenario triggers it.\n\n### 3. SharedContentSearchViewModel â€” stale _searchQuery across sequential awaits\n`lib/viewmodels/shared_content/shared_content_search_viewmodel.dart:295â€“370`\n\n`_searchQuery` is a field, not local copy. Between three sequential `await _searchContent(...)` calls, a keystroke changes it. Recipe results from \"piz\" mixed with menu results from \"pizza\".\n\n## Why It Matters\nFinding 1 causes OOM. Finding 2 causes stale presence. Finding 3 causes incorrect search results.\n\n## Suggested Fix\n1. Snapshot `_currentMemoryUsage` or use Completer-based lock\n2. Replace `asyncMap` + `await for` with `Rx.combineLatest`\n3. Snapshot `final query = _searchQuery` at method start; cancel in-flight search on new query"
        },
        {
          "findingKey": "trivial-test-assertions-false-coverage",
          "title": "Fix 55+ trivial test assertions giving false coverage confidence across 15 test files",
          "labels": [
            "test-gap"
          ],
          "priority": 2,
          "description": "## Finding\n\n### expect(true, isTrue) placeholder assertions (30+ tests)\nTests that always pass regardless of implementation:\n- `notification_service_test.dart:148,198,220,245,267,309` â€” 6 tests\n- `messaging_service_test.dart:816,1330,1340,1411,1464` â€” 5 tests (3 for unimplemented methods)\n- `recipe_social_stats_test.dart:746,801` â€” rating aggregates\n- `realtime_event_handler_test.dart:301,317` â€” notifications\n- `startup_optimization_manager_test.dart:116,125` â€” service registration\n- `performance_monitoring_service_test.dart:129,138` â€” initialize\n- `intelligent_cache_manager_test.dart:249,322` â€” initialize + preload\n- Plus 7 more files with 1 placeholder each\n\n### url_import_strategy_test.dart â€” 29 tests with only expect(strategy, isNotNull)\nLines 207â€“458: Tests named for specific behaviors but only assert non-null. Entire extraction pipeline has zero real assertions.\n\n### Tests for unimplemented methods\n`messaging_service_test.dart:1334,1404,1457` â€” tests for addReaction/removeReaction/updatePresence that don't exist.\n\n## Why It Matters\nThese create phantom coverage. url_import_strategy_test alone has 29 tests that pass even if the entire implementation were deleted.\n\n## Suggested Fix\n1. Replace `expect(true, isTrue)` with actual state/interaction assertions\n2. url_import_strategy_test: assert extracted title, ingredients, instructions\n3. Delete tests for unimplemented methods\n4. Replace `isA<String?>()` with content verification"
        },
        {
          "findingKey": "flaky-timing-tests-skipped-tests",
          "title": "Fix 13+ flaky timing tests and 20+ improperly skipped tests",
          "labels": [
            "test-gap"
          ],
          "priority": 3,
          "description": "## Finding\n\n### Flaky: Future.delayed without fakeAsync in unit tests\n- `collaborative_status_viewmodel_test.dart` â€” 13Ã— `Future.delayed(100-200ms)` for async state propagation\n- `rate_limiter_test.dart:71,84,432` â€” 3Ã— `Future.delayed(1100ms)` for token bucket refill\n\n### Improperly skipped with no resolution plan\n- `firebase_connectivity_repository_test.dart` â€” 7 tests skip: 'Requires Flutter bindings'\n- `friend_relationship_repository_test.dart` â€” 3 tests skip: 'FieldValue.increment'\n- `friend_category_repository_test.dart` â€” 3 tests skip: 'FieldValue.arrayUnion/Remove'\n- `group_invitation_repository_test.dart` â€” 4 tests skip: 'FieldValue.serverTimestamp'\n- `startup_optimization_manager_test.dart` â€” 3 tests skip: 'singleton state persistence'\n\n### Singleton state leaking between tests\n- `startup_optimization_manager_test.dart` â€” singleton retains initialized state\n\n## Why It Matters\nFlaky tests erode CI trust. Skipped tests create false coverage confidence.\n\n## Suggested Fix\n1. Replace Future.delayed with fakeAsync + async.elapse()\n2. Add TestWidgetsFlutterBinding.ensureInitialized() for connectivity tests\n3. Use fake_cloud_firestore FieldValue support or mock at service layer\n4. Add singleton reset capability"
        },
        {
          "findingKey": "widget-architecture-duplication-nesting-setstate",
          "title": "Fix widget architecture: duplicated card subtrees, deep nesting, context threading, setState anti-patterns (10+ views)",
          "labels": [
            "tech-debt",
            "social",
            "recipe"
          ],
          "priority": 3,
          "description": "## Finding\n\n### Duplicated widget subtrees\n1. `shared_recipe_card.dart`, `shared_menu_card.dart`, `shared_shopping_list_card.dart` â€” identical outer skeleton and _buildHeader across all three\n2. `privacy_policy_view.dart`, `terms_of_service_view.dart`, `community_guidelines_view.dart` â€” identical _buildContactButton footer\n3. `notification_preferences_view.dart:118,165,220,333,358` â€” SwitchListTile thumbColor block duplicated 5Ã—\n\n### Deep nesting (10+ levels)\n4. `recipe_detail_content.dart:183â€“279` â€” ingredient row 11 levels deep\n5. `recipe_detail_view.dart:94â€“563` â€” main build 12+ levels\n\n### Context threading through static methods\n6. shared_*_card.dart â€” context passed through 4 levels of static method calls\n\n### setState anti-patterns\n7. `recipe_detail_view.dart:539,548` â€” empty setState(() {}) forces full view rebuild\n8. `mfa_settings_view.dart:70,121â€“130,134` â€” 3Ã— setState in single async method\n\n### Unnecessary StatefulWidgets\n9. `emoji_reaction_picker.dart:68â€“111` â€” StatefulWidget for single bool hover\n10. `search_input_widget.dart:17â€“75` â€” StatefulWidget solely for empty setState on text change\n\n## Why It Matters\nDuplicated subtrees mean bugs fixed in one card aren't fixed in others. Deep nesting defeats rebuild optimization. Empty setState causes full-subtree rebuilds.\n\n## Suggested Fix\n1. Extract SharedContentCardShell + SharedContentCardHeader\n2. Extract LegalContactFooter widget\n3. Extract _butlerySwitchTheme() helper\n4-5. Extract _IngredientRow and _RecipeTitleSection\n6. Convert static classes to proper StatelessWidget subclasses\n7-8. Use ViewModel notifyListeners instead of parent setState\n9-10. Convert to StatelessWidget with AnimatedBuilder"
        },
        {
          "findingKey": "service-contract-findByTitle-notifications-fcm",
          "title": "Fix service contract bugs: findByTitle never matches, notification prefs dual collection, FCM token deletion (6 issues)",
          "labels": [
            "Bug",
            "backend"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. findByTitle() never matches â€” duplicate detection broken\n`lib/repositories/firebase/firebase_recipe_repository.dart:567â€“577`\n\nQuery uses lowercase but toFirestore() stores original case. Firestore exact comparison â†’ never matches.\n\n### 2. Notification preferences in two different Firestore collections\nLegacy: `notification_preferences` vs New: `user_notification_preferences`. UI writes to legacy, interface reads from new â†’ always defaults.\n\n### 3. removeFCMToken ignores token parameter, deletes ALL tokens\n`lib/repositories/firebase/firebase_notifications_repository.dart:374â€“378`\n\nMulti-device users lose push for all devices when one signs out.\n\n### 4. setActiveList is in-memory only\n`lib/repositories/firebase/firebase_shopping_repository.dart:292â€“294`\n\nLost on cold start. getActiveList() returns null.\n\n### 5. toFirestore() writes DateTime, fromFirestore() reads Timestamp\n`notifications_repository.dart:143` and `ratings_repository.dart:78`. Latent crash on base class create() path.\n\n### 6. Two parallel NotificationRepository classes\nLegacy (not DI-registered) vs new (DI-registered). NotificationService bypasses DI.\n\n## Why It Matters\nFinding 1: All import duplicate detection broken. Finding 2: Notification prefs never persist. Finding 3: Multi-device push broken on logout.\n\n## Suggested Fix\n1. Store normalizedTitle in toFirestore(), query against that\n2. Consolidate to one collection; delete legacy repository\n3. Use FieldValue.arrayRemove([token]) for specific token\n4. Persist _activeListId via SharedPreferences\n5. Use Timestamp.fromDate() in toFirestore\n6. Merge into single DI-registered repository"
        },
        {
          "findingKey": "accessibility-contrast-autofocus-responsive-focus",
          "title": "Fix accessibility gaps: auth contrast fail, missing autofocus, responsive wrappers, focus management (14 locations)",
          "labels": [
            "Bug",
            "account"
          ],
          "priority": 2,
          "description": "## Finding\n\n### WCAG contrast failure\n1. `auth_view.dart:128â€“131` â€” Tagline ~1.9:1 contrast, needs 4.5:1\n\n### Missing autofocus\n2. `auth_view.dart:543â€“553` â€” Password reset dialog no autofocus\n\n### Link semantics\n3. `auth_view.dart:439â€“472` â€” GestureDetector on bare Text, no tap target, no Semantics(link: true)\n\n### Missing responsive wrappers\n4. `faq_view.dart:28â€“30` â€” No Center + ConstrainedBox\n5. `onboarding_view.dart:61` + `onboarding_allergen_page.dart:65` â€” No max-width, fixed crossAxisCount: 2\n\n### Keyboard/focus\n6. `account_security_view.dart:148â€“215` â€” 5 TextFields no Tab progression\n7. `tag_detail_view.dart:75â€“79` â€” Edit mode no requestFocus()\n\n### Tap targets (new locations)\n8-10. cooking_mode close (40px), stepper (36px), Lagat idag shrinkWrap\n\n### Cooking mode\n11. Non-Expanded Row overflows at 1.3x text scale\n12. Forces landscape with no portrait fallback\n\n### Screen reader\n13. `mina_recept_view.dart:520â€“539` â€” Selection state not in Semantics\n14. `auth_view.dart:105â€“108` â€” Decorative image not excluded\n\n## Why It Matters\nContrast failure affects all users. Missing keyboard nav breaks web. 36px tap targets fail mobile accessibility.\n\n## Suggested Fix\n1. Increase tagline opacity or use cs.onPrimary\n2. Add autofocus: true\n3. Replace GestureDetector with TextButton + Semantics\n4-5. Add Center + ConstrainedBox\n6. Add FocusNode chain + textInputAction\n7. Call requestFocus() after setState\n8-10. Increase to 48dp\n11. Wrap label in Expanded\n13. Add Semantics(selected: isSelected)\n14. Add excludeFromSemantics: true"
        }
      ]
    },
    {
      "scanFocus": "ultrathink-deep-scan-17-cloud-functions-firestore-rules-lifecycle-platform-parsing-di",
      "tickets": [
        {
          "findingKey": "cloud-functions-auth-validation-gaps",
          "title": "Fix Cloud Functions auth gaps: 4 missing admin checks, rate limit key mismatch, imageUrl SSRF, LLM cost abuse (9 issues)",
          "labels": [
            "security",
            "backend"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. Missing admin authorization (4 functions)\n`functions/src/admin/` â€” `cleanupOrphanedData`, `migrateUserData`, `forceReindex`, `bulkDeleteUsers` check `auth.uid` exists but never verify admin role/claim. Any authenticated user can call them.\n\n### 2. Rate limiter key mismatch\n`functions/src/middleware/rateLimiter.ts` â€” key uses `req.ip` but behind Cloud Run proxy, `req.ip` is always the proxy IP. All users share one rate limit bucket.\n\n### 3. imageUrl SSRF in recipe import\n`functions/src/import/processRecipeImport.ts` â€” accepts user-supplied `imageUrl`, fetches it server-side with no URL validation. Can probe internal GCP metadata endpoint (`169.254.169.254`).\n\n### 4. LLM API cost abuse\n`functions/src/ai/generateTags.ts` â€” no per-user daily quota. Single user can trigger unlimited OpenAI API calls.\n\n### 5-9. Input validation gaps\nMissing field length limits on recipe title (can store 1MB string), missing Content-Type validation, no request body size limit on 3 endpoints.\n\n## Why It Matters\nAdmin functions callable by any user = full data wipe possible. SSRF = GCP credential theft. No LLM quota = unbounded cost.\n\n## Suggested Fix\n1. Add `verifyAdminClaim(context)` helper checking custom claims\n2. Use `req.headers['x-forwarded-for']` or Cloud Run `X-Cloud-Trace-Context`\n3. Validate URL scheme (https only) + block private IP ranges\n4. Add per-user daily call counter in Firestore\n5-9. Add express-validator middleware with field length limits"
        },
        {
          "findingKey": "cloud-functions-idempotency-error-handling",
          "title": "Fix Cloud Functions: idempotency failures, OOM cleanup, N+1 queries, wrong timeout config (9 issues)",
          "labels": [
            "Bug",
            "backend"
          ],
          "priority": 2,
          "description": "## Finding\n\n### 1. Non-idempotent onCreate triggers\n`functions/src/triggers/onRecipeCreate.ts`, `onUserCreate.ts` â€” Cloud Functions can retry on timeout. These create denormalized docs without checking if they already exist â†’ duplicate data on retry.\n\n### 2. cleanupOrphanedData fetches all docs into memory\n`functions/src/admin/cleanupOrphanedData.ts` â€” `const allRecipes = await db.collection('recipes').get()` loads entire collection. Will OOM at ~10k recipes.\n\n### 3. N+1 query in generateWeeklyDigest\n`functions/src/scheduled/weeklyDigest.ts` â€” loops users, fetches recipes one-by-one inside loop instead of batch query.\n\n### 4. Wrong timeout/memory on v2 functions\n3 functions set `timeoutSeconds: 60` (v1 syntax) in v2 `onCall` options â€” silently ignored, gets default 60s which may be too short for batch operations.\n\n### 5-9. Error handling gaps\n5 functions catch errors but return `{success: true}` anyway, 2 functions don't catch at all (unhandled rejection â†’ cold start on next call).\n\n## Why It Matters\nNon-idempotent triggers cause duplicate data. OOM crashes cleanup function. N+1 causes weekly digest timeout.\n\n## Suggested Fix\n1. Check document existence before create in triggers\n2. Use paginated query with `startAfter` cursor\n3. Batch user recipe queries with `where('userId', 'in', chunk)`\n4. Use `timeoutSeconds` in `GlobalOptions` or per-function `HttpsOptions`\n5-9. Return error status on catch, add try-catch to unguarded functions"
        },
        {
          "findingKey": "firestore-rules-missing-camelcase-denorm-collections",
          "title": "Fix CRITICAL: Firestore rules missing for camelCase denorm collections + 8 more collections (menu/shopping sharing broken)",
          "labels": [
            "security",
            "backend",
            "social",
            "shopping"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. Denormalized collections use camelCase, rules use snake_case\n`firestore.rules` has rules for `shared_recipes`, `shared_menus`, `shared_shopping_lists` but app code writes to `sharedRecipes`, `sharedMenus`, `sharedShoppingLists`. Rules never match â†’ default deny â†’ all sharing silently fails.\n\n### 2. 8 collections with no rules at all\n- `userPresence` â€” presence writes blocked\n- `recipeEngagements` â€” likes/comments blocked\n- `menuTemplates` â€” menu template CRUD blocked\n- `shoppingListItems` (subcollection) â€” item add/remove blocked\n- `friendCategories` â€” category management blocked\n- `groupInvitations` â€” group invite flow blocked\n- `userNotificationPreferences` â€” notification prefs blocked\n- `recipeImportQueue` â€” import queue blocked\n\n### 3. Collection naming inconsistency\nMix of camelCase (`userPresence`) and snake_case (`user_profiles`) across codebase. No naming convention enforced.\n\n## Why It Matters\nCRITICAL: All social sharing features (recipes, menus, shopping lists) are silently broken in production. Firestore default-deny means writes succeed client-side (optimistic) but fail server-side.\n\n## Suggested Fix\n1. Add rules for camelCase collection names matching app code\n2. Add rules for all 8 missing collections with proper auth checks\n3. Document naming convention, migrate to consistent style in next major version"
        },
        {
          "findingKey": "firestore-rules-logic-bugs-reactions-ownership",
          "title": "Fix Firestore rule logic bugs: menu reads permanently denied, reactions blocked, ownership hijack (7 issues)",
          "labels": [
            "security",
            "backend",
            "social"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. Menu read rule permanently denies after unshare\n`firestore.rules:847` â€” `resource.data.sharedWith` checked but field removed on unshare instead of set to empty array. `resource.data.sharedWith` â†’ field doesn't exist â†’ rule throws â†’ deny. Owner also locked out.\n\n### 2. Reactions subcollection create blocked\n`firestore.rules:923` â€” requires `request.resource.data.userId == request.auth.uid` but client sends `authorId` field instead.\n\n### 3. Recipe ownership hijack via update\n`firestore.rules:312` â€” update rule checks `request.auth.uid == resource.data.userId` but doesn't prevent changing `userId` field. User can transfer ownership to themselves for any recipe they can update.\n\n### 4. Comment delete allows any participant\n`firestore.rules:945` â€” delete rule checks group membership but not comment authorship. Any group member can delete anyone's comments.\n\n### 5-7. Minor logic issues\n5. `get()` in rules counts toward quota but result uncached across rule evaluations\n6. `hasAll` used where `hasAny` intended for permission check\n7. Timestamp validation allows future dates (no `<= request.time` check)\n\n## Why It Matters\nFinding 1: Menus permanently inaccessible after unshare. Finding 3: Any collaborator can steal recipe ownership.\n\n## Suggested Fix\n1. Check `'sharedWith' in resource.data && request.auth.uid in resource.data.sharedWith` with existence guard\n2. Change field name to `authorId` in rule or `userId` in client\n3. Add `!('userId' in request.resource.data) || request.resource.data.userId == resource.data.userId` to prevent ownership change\n4. Add `resource.data.authorId == request.auth.uid || isOwner()` to comment delete\n5-7. Cache get() results, fix hasAllâ†’hasAny, add timestamp bounds"
        },
        {
          "findingKey": "app-lifecycle-deeplink-fcm-auth-critical",
          "title": "Fix app lifecycle: deep link dropped on cold start, FCM navigation unwired, auth token expiry swallowed (3 critical bugs)",
          "labels": [
            "Bug",
            "backend",
            "account"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. Deep link dropped on cold start\n`lib/services/deep_link_service.dart:45â€“67` â€” `getInitialLink()` called in `initialize()` but result stored before `NavigatorState` is available. Link processed, `_pendingDeepLink` set, but no widget is listening yet. After widget tree builds and subscribes, pending link already consumed.\n\n### 2. FCM notification tap navigation unwired\n`lib/services/notification/fcm_service.dart:89â€“112` â€” `onMessageOpenedApp` handler parses payload and calls `_handleNotificationTap()` which is a stub that only logs. User taps notification â†’ app opens to home screen, not the relevant content.\n\n### 3. Auth token expiry swallowed\n`lib/services/auth/auth_service.dart:234â€“241` â€” `onIdTokenChanged` catches `FirebaseAuthException` for expired token but only logs warning. No force-refresh attempted, no user redirect to login. User sees stale data until app restart.\n\n## Why It Matters\nDeep links from shared recipes/menus don't work on cold start (most common case). Notification taps don't navigate. Expired auth tokens cause silent data staleness.\n\n## Suggested Fix\n1. Queue pending deep link, process after first frame via `WidgetsBinding.instance.addPostFrameCallback`\n2. Implement `_handleNotificationTap()` with route resolution from payload type\n3. Call `user.getIdToken(true)` on expiry, redirect to login on failure"
        },
        {
          "findingKey": "app-lifecycle-autosave-presence-background",
          "title": "Fix app lifecycle: auto-save lost on background kill, presence ghost, offline sync race, FCM stub (4 issues)",
          "labels": [
            "Bug",
            "backend"
          ],
          "priority": 2,
          "description": "## Finding\n\n### 1. Auto-save lost on background kill\n`lib/viewmodels/recipe_form_viewmodel.dart:89â€“95` â€” dirty form state only saved on explicit save button. `AppLifecycleState.paused` handler missing. OS kills backgrounded app â†’ unsaved edits lost.\n\n### 2. Presence ghost after app kill\n`lib/services/presence_service.dart:156â€“170` â€” `onDisconnect()` handler set on Firebase RTDB but only for graceful disconnect. Force-kill leaves presence as 'online' indefinitely. No server-side TTL.\n\n### 3. Offline sync race on reconnect\n`lib/services/unified/modules/personal_recipe_module.dart:612â€“640` â€” `_pendingWrites` queue replayed on connectivity change, but no ordering guarantee. If user edited recipe A then B offline, B might sync before A, and A's sync overwrites B's `lastModified`.\n\n### 4. FCM token refresh stub\n`lib/services/notification/fcm_service.dart:134â€“138` â€” `onTokenRefresh` stream listened but handler is `TODO: implement`. Old token becomes invalid, push notifications stop until app reinstall.\n\n## Why It Matters\nAuto-save loss frustrates users. Ghost presence misleads collaborators. Token refresh stub breaks push after token rotation.\n\n## Suggested Fix\n1. Add `WidgetsBindingObserver` + save dirty state on `paused`/`detached`\n2. Add server-side presence TTL (Cloud Function cron: mark offline if no heartbeat in 5min)\n3. Add sequence number to pending writes, replay in order\n4. Implement token refresh: call `updateFCMToken(newToken)` on repository"
        },
        {
          "findingKey": "ios-platform-config-critical",
          "title": "Fix iOS critical misconfigs: wrong bundle ID in Firebase config, deployment target 13.0, https URL scheme, missing entitlements (5 issues)",
          "labels": [
            "Bug",
            "backend"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. GoogleService-Info.plist bundle ID mismatch\n`ios/Runner/GoogleService-Info.plist:BUNDLE_ID` = `com.example.butlery` but `ios/Runner.xcodeproj/project.pbxproj:PRODUCT_BUNDLE_IDENTIFIER` = `com.butlery.app`. Firebase SDK will fail to initialize on real iOS device.\n\n### 2. iOS deployment target 13.0\n`ios/Podfile:platform :ios, '13.0'` â€” Firebase iOS SDK 11.x requires iOS 15.0+. Pod install will fail or produce runtime crashes.\n\n### 3. URL scheme uses https\n`ios/Runner/Info.plist:CFBundleURLSchemes` includes `https` â€” this is reserved by the system. Should use custom scheme like `butlery://`.\n\n### 4. Missing Associated Domains entitlement\nNo `Runner.entitlements` file with `applinks:` domain. Universal links (deep links) won't work on iOS.\n\n### 5. Missing push notification entitlement\nNo `aps-environment` in entitlements. FCM push notifications will silently fail on iOS.\n\n## Why It Matters\nBundle ID mismatch = Firebase crash on launch = app unusable on iOS. Deployment target too low = pod install failure.\n\n## Suggested Fix\n1. Update GoogleService-Info.plist BUNDLE_ID to `com.butlery.app`\n2. Update Podfile to `platform :ios, '15.0'`\n3. Replace `https` URL scheme with `butlery`\n4. Add `Runner.entitlements` with `applinks:butlery.app`\n5. Add `aps-environment` = `production` to entitlements"
        },
        {
          "findingKey": "platform-config-region-headers-permissions",
          "title": "Fix platform config: Cloud Functions region mismatch (NOT_FOUND), missing security headers, storage permission (4 issues)",
          "labels": [
            "Bug",
            "backend"
          ],
          "priority": 2,
          "description": "## Finding\n\n### 1. Cloud Functions region mismatch\n`lib/services/cloud_functions_service.dart:15` â€” `FirebaseFunctions.instanceFor(region: 'us-central1')` but `functions/src/index.ts` deploys to `europe-west1`. All callable function invocations return NOT_FOUND.\n\n### 2. Missing web security headers\n`web/index.html` â€” no Content-Security-Policy, X-Frame-Options, or X-Content-Type-Options headers. App embeddable in iframe (clickjacking).\n\n### 3. Android storage permission for image upload\n`android/app/src/main/AndroidManifest.xml` â€” missing `READ_MEDIA_IMAGES` permission (Android 13+). Image picker will fail silently on newer devices.\n\n### 4. Firebase Storage CORS not configured\nNo `cors.json` deployed to Firebase Storage bucket. Web uploads may fail with CORS errors.\n\n## Why It Matters\nRegion mismatch means ALL Cloud Function calls fail in production (NOT_FOUND). Missing storage permission breaks image upload on Android 13+.\n\n## Suggested Fix\n1. Change region to `europe-west1` in `cloud_functions_service.dart`\n2. Add security headers via Firebase Hosting `firebase.json` headers config\n3. Add `READ_MEDIA_IMAGES` + `READ_MEDIA_VIDEO` permissions\n4. Deploy `cors.json` with allowed origins to Firebase Storage"
        },
        {
          "findingKey": "parsing-oom-charset-schema-handling",
          "title": "Fix recipe parsing: no HTTP response size limit (OOM), no charset detection, JSON-LD @type array missed, instructions discarded (6 issues)",
          "labels": [
            "Bug",
            "import",
            "parsing"
          ],
          "priority": 2,
          "description": "## Finding\n\n### 1. No HTTP response size limit\n`lib/services/import/url_import_strategy.dart:78â€“92` â€” `http.get(url)` reads entire response into memory. Malicious or misconfigured URL serving 500MB file â†’ OOM crash.\n\n### 2. No charset detection\n`lib/services/import/url_import_strategy.dart:95` â€” `response.body` assumes UTF-8 but many recipe sites use ISO-8859-1 or Windows-1252. Swedish characters (Ã¥, Ã¤, Ã¶) corrupted on import from older sites.\n\n### 3. JSON-LD @type as array missed\n`lib/services/parsing/recipe_parser_service.dart:234â€“240` â€” checks `jsonLd['@type'] == 'Recipe'` but Schema.org allows `@type: ['Recipe', 'HowTo']`. Array form silently skipped â†’ falls back to less accurate HTML scraping.\n\n### 4. HowToSection instructions discarded\n`lib/services/parsing/recipe_parser_service.dart:312â€“330` â€” `recipeInstructions` handler processes `HowToStep` but ignores `HowToSection` wrapper. Grouped instructions (e.g., 'For the dough:', 'For the filling:') lose all steps inside sections.\n\n### 5. Yield field parsing ignores servings format\n`lib/services/parsing/recipe_parser_service.dart:198` â€” `recipeYield: '4-6 servings'` parsed as string, not extracted to numeric range.\n\n### 6. Duration parsing breaks on non-ISO format\n`lib/services/parsing/recipe_parser_service.dart:205` â€” expects ISO 8601 (`PT30M`) but some sites use `30 min` or `30 minutes`. Parse fails silently â†’ null cook time.\n\n## Why It Matters\nOOM from unbounded response is a DoS vector. Charset corruption affects all Swedish recipe sites using legacy encoding.\n\n## Suggested Fix\n1. Use `http.Client` with `StreamedResponse` + max bytes check (10MB limit)\n2. Read `Content-Type` charset header, use `dart:convert` Encoding.getByName\n3. Check `jsonLd['@type'] is List ? jsonLd['@type'].contains('Recipe') : jsonLd['@type'] == 'Recipe'`\n4. Recursively unwrap HowToSection â†’ extract HowToStep children\n5. Regex extract numeric portion from yield string\n6. Add fallback duration parser for common non-ISO formats"
        },
        {
          "findingKey": "di-lifecycle-order-bugs-singleton-lifecycle",
          "title": "Fix DI lifecycle: cross-module dependency order bugs (StateError at bootstrap) + singleton streams not reset on logout (8 issues)",
          "labels": [
            "Bug",
            "backend"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. Cross-module dependency order not enforced\n`lib/di/` modules declare `priority` but 3 modules have equal priority with cross-dependencies. `SocialModule` (priority 30) depends on `UnifiedRecipeService` from `RecipeModule` (priority 30). Registration order within same priority is insertion order â†’ works by accident. Adding a new module at priority 30 can break bootstrap.\n\n### 2. Circular dependency: NotificationService â†” FriendsService\n`NotificationService` constructor takes `FriendsService` for friend-request notifications. `FriendsService` constructor takes `NotificationService` for sending notifications. Both registered in `SocialModule` â†’ one must be registered first â†’ the other's dependency is null at construction.\n\n### 3. Singleton streams not reset on logout\n`UnifiedRecipeService`, `PresenceService`, `IntelligentCacheManager` â€” registered as singletons in DI. On logout, `_recipes` map and stream subscriptions retained. On re-login as different user, previous user's data visible briefly.\n\n### 4. dispose() never called on DI singletons\n`lib/di/di_container.dart:reset()` calls `GetIt.instance.reset()` but doesn't call `dispose()` on ChangeNotifier singletons first. StreamControllers and listeners leak.\n\n### 5-8. Registration gaps\n5. `AnalyticsService` registered but depends on unregistered `ConsentManager`\n6. `ImageUploadService` registered in `MediaModule` but `StorageRepository` in `FirebaseModule` â€” no declared dependency\n7. Two `RecipeRepository` registrations (one in `FirebaseModule`, one in `RecipeModule`) â€” last wins silently\n8. `TaggingService` registered as factory but used as singleton by callers caching the instance\n\n## Why It Matters\nFinding 1: Bootstrap crash on innocent module addition. Finding 3: Cross-user data leakage on logout/re-login. Finding 4: Resource leaks accumulate across auth cycles.\n\n## Suggested Fix\n1. Enforce dependency ordering: modules declare `dependsOn: [ModuleName]`, DI container topologically sorts\n2. Break circular dep with lazy initialization or event bus\n3. Add `resetUserState()` method to stateful singletons, call on logout\n4. Call `dispose()` on all ChangeNotifier singletons before `reset()`\n5-8. Audit and fix registration: remove duplicate, declare dependencies, convert TaggingService to singleton"
        }
      ]
    },
    {
      "scanFocus": "ultrathink-deep-scan-18-search-social-groups-image-pipeline-auth-menu-shopping-onboarding",
      "tickets": [
        {
          "findingKey": "search-debounce-stream-mixin-critical",
          "title": "Fix search infrastructure: debounce Completer leak, static mixin state, watchRecipes 50-doc cap (4 critical bugs)",
          "labels": [
            "Bug",
            "recipe"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. executeDebounced leaks Completer futures on rapid keypresses\n`lib/core/mixins/async_operation_mixin.dart:133â€“159`\n\nEach call creates a new `Completer<T?>`, then cancels the previous timer. Cancelled Completers are never completed â€” they leak, awaiting forever. `RecipeQueryViewModel.updateSearchQuery` (line 104) `await`s this. 10 rapid keypresses = 9 permanently abandoned Completers holding references.\n\n### 2. watchRecipes stream silently truncates to 50 recipes\n`lib/repositories/firebase/firebase_recipe_repository.dart:352â€“360`\n\nThe live stream feeding `UnifiedRecipeService.recipes` has `.limit(50)` ordered by `updatedAt desc`. Users with 51+ recipes never see older ones in any filter/search/sort. UI pagination operates on the already-truncated list.\n\n### 3. Static _executionTimes is process-global â€” leaks across instances and auth sessions\n`lib/core/mixins/async_operation_mixin.dart:360â€“368`\n\n`static final Map<String, DateTime> _executionTimes` shared across ALL mixin instances. Never cleared on logout. Throttle timestamps from one user persist into next session, silently suppressing operations.\n\n### 4. Debounce does not cancel in-flight async operation\n`lib/core/mixins/async_operation_mixin.dart:46â€“60`\n\n`executeDebounced` cancels the pending timer, but if a search is already executing inside `executeNamedOperation`, the next debounced search hits `_activeOperations.contains('recipeSearch')` and throws `StateError`. User sees no results update.\n\n## Why It Matters\nFinding 1: Memory leak and hung awaits. Finding 2: Users with 51+ recipes have silently incomplete search/filter results. Finding 3: Cross-user state leakage.\n\n## Suggested Fix\n1. Complete old Completer with `null` before replacing it\n2. Remove `.limit(50)` from `watchRecipes`; use cursor-based pagination\n3. Change `_executionTimes` from `static final` to instance field; clear in `dispose()`\n4. Pass `allowConcurrent: true` or cancel in-flight operations properly"
        },
        {
          "findingKey": "search-quality-filter-state-normalization",
          "title": "Fix search quality: Algolia empty owner, Firestore fallback unscoped, filter state leak on logout, date range off-by-one (13 issues)",
          "labels": [
            "Bug",
            "recipe"
          ],
          "priority": 2,
          "description": "## Finding\n\n### 1. Algolia ownerDisplayName always empty\n`lib/repositories/algolia/algolia_search_repository.dart:299` â€” hardcodes `ownerDisplayName: ''`. Author-based search never matches.\n\n### 2. Firestore fallback search returns any public recipe, unscoped\n`lib/repositories/firebase/firebase_search_repository.dart:35â€“47` â€” no user scope, `page` parameter ignored, client-side text filtering after limit applied.\n\n### 3. Filter state not cleared on logout\n`lib/viewmodels/recipe_list_viewmodel.dart` â€” allergen/dietary/tag filters survive logout. Multi-user device shows previous user's filters.\n\n### 4. Archive search has no debounce\n`lib/viewmodels/archive/archive_search_manager.dart:48â€“52` â€” `updateSearch` triggers full recompute + rebuild on every keystroke.\n\n### 5. Archive cache uses Set.toString() comparison\n`lib/viewmodels/archive/archive_search_manager.dart:84` â€” Set ordering not guaranteed in `toString()`, causes unnecessary cache misses.\n\n### 6. Search history accumulates every keystroke\n`lib/viewmodels/shared_content/shared_content_search_viewmodel.dart:268â€“273` â€” typing 'pasta' inserts 'p','pa','pas','past','pasta' into history.\n\n### 7. filteredResults computed 3x per build cycle\n`lib/viewmodels/shared_content/shared_content_search_viewmodel.dart:156â€“223` â€” no caching; `resultsByType` and `resultCounts` each re-call it.\n\n### 8. DateRange.thisWeek off-by-one\n`lib/viewmodels/shared_content/shared_content_search_viewmodel.dart:194â€“196` â€” `weekStart` uses time-of-day from `now`, not midnight. Items before that time on Monday excluded.\n\n### 9. Friends search cache has no TTL\n`lib/viewmodels/friends/friends_search_manager.dart:110â€“117` â€” stale results never invalidated.\n\n### 10-13. Swedish normalization gaps\n`recipe_query_viewmodel.dart:84â€“98` uses raw `contains()` without normalization. `search_service.dart:230â€“259` suggestions lack normalization. `search_repository.dart:229â€“259` Algolia filter strings not escaped. Firestore search pagination always returns page 0.\n\n## Why It Matters\nAlgolia author search broken. Firestore fallback returns arbitrary data. Filter leak = cross-user privacy issue.\n\n## Suggested Fix\n1. Thread displayName to Algolia indexing\n2. Add user scope to Firestore search; implement pagination\n3. Clear filters on auth state change\n4-5. Add debounce; use SetEquality\n6. Move history insertion to after debounce fires\n7. Cache filteredResults\n8. Use midnight DateTime for week start\n9. Add cache TTL\n10-13. Use SwedishCharacterNormalizer everywhere; escape Algolia filters"
        },
        {
          "findingKey": "social-group-security-ownership-permissions",
          "title": "Fix CRITICAL social group security: any member can hijack ownership, validateReadPermission always true, members can overwrite all fields (6 issues)",
          "labels": [
            "security",
            "social"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. Any group member can transfer ownership â€” no server-side guard\n`lib/viewmodels/social_group_detail_viewmodel.dart:302â€“334` + `firestore.rules:205â€“208`\n\nFirestore update rule allows any member listed in `friendUserIds` to update ANY field including `ownerId`. Client-side `isAdmin` check is bypassable. Any group member can transfer ownership to themselves or anyone.\n\n### 2. validateReadPermission returns true for all group-shared content\n`lib/repositories/firebase/firebase_social_sharing_repository.dart:107â€“110`\n\nComment says 'delegate to group-level security' but just returns `true`. Any authenticated user with a document ID can read group-shared content without being a member.\n\n### 3. All four validate*Permission methods return true unconditionally\n`lib/repositories/firebase/friends/friend_category_repository.dart:115â€“144`\n\nAll CRUD permissions bypass. Callers using generic `create()`/`delete()` base class methods have zero permission checks.\n\n### 4. SharingPermissions.canEdit stored but never enforced\n`lib/models/shared_content.dart:183â€“289` + `firestore.rules:346â€“351`\n\nFirestore update rule for shared_recipes allows members to update non-ownership fields regardless of `allowCollaboration` flag.\n\n### 5. addMember has no ownership check at Dart layer\n`lib/repositories/firebase/base_shared_content_repository.dart:340â€“374`\n\nAny caller can add any user as member of any content. Only Firestore rules enforce ownership.\n\n### 6. Members can do full document set() overwriting all group fields\n`lib/repositories/firebase/friends/friend_category_repository.dart:147â€“179`\n\n`saveCategory` uses `.set(category.toFirestore())` â€” full overwrite including `ownerId`, `name`, `friendUserIds`. Any member can rename group, remove others, or set themselves as owner.\n\n## Why It Matters\nFinding 1+6: Any group member can steal ownership. Finding 2: Group content visible to non-members. Finding 4: Edit permissions meaningless.\n\n## Suggested Fix\n1. Add Firestore rule: only current owner can change `ownerId`\n2. Implement actual group membership check before returning true\n3. Implement real ownership checks in validate* methods\n4. Add `resource.data.allowCollaboration == true` to member update rule\n5. Fetch content doc and verify ownership before addMember\n6. Use targeted `.update()` for non-owner members; restrict to `friendUserIds` and `updatedAt` only"
        },
        {
          "findingKey": "social-group-invitation-membership-stubs",
          "title": "Fix social group flow: acceptInvitation doesn't add member, cancel blocked, remove stub empty, rules mismatch (10 issues)",
          "labels": [
            "Bug",
            "social"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. acceptInvitation only updates status â€” never adds user to group\n`lib/repositories/firebase/friends/group_invitation_repository.dart:280â€“291`\n\nAccepting changes invitation `status` to 'accepted' but never adds user to `friendUserIds` in the group document. User appears accepted but is not a member.\n\n### 2. cancelInvitation blocked by BOTH Dart and Firestore layers\n`lib/repositories/firebase/friends/group_invitation_repository.dart:306â€“317` + `firestore.rules:312â€“315`\n\nDart `updateInvitation()` throws if caller != `toUserId`. Firestore rules only allow `toUserId` to update. Status 'cancelled' not in allowed values. Invitation cancellation is completely dead functionality.\n\n### 3. removeFriendFromCategoryInternal has empty body\n`lib/services/unified/friends/friends_internal_operations.dart:173`\n\n`void removeFriendFromCategoryInternal(String friendId, String categoryId) {}` â€” complete no-op. Remove-from-group silently fails.\n\n### 4. syncFriendToFirebase is a documented landmine still in codebase\n`lib/services/unified/friends/friends_firebase_sync.dart:62â€“97`\n\nDocstring warns NOT to use it (non-atomic, wrong schema, skips friendsCount). Should be deleted.\n\n### 5. shared_content Firestore rules use fromUserId/toUserId but model writes ownerId/sharedWithUserIds\n`firestore.rules:1176â€“1200` + `lib/models/shared_content.dart`\n\nRules check fields that don't exist on real documents. All read access to shared_content is effectively denied.\n\n### 6. No self-invite prevention in group_invitations create rule\n`firestore.rules:307â€“310` â€” no `fromUserId != toUserId` check.\n\n### 7. GroupEventBus has no group ID â€” fires reload for all open groups\n`lib/viewmodels/social_group_detail_viewmodel.dart:140â€“158`\n\n### 8. updateSharingPermissions â€” two non-atomic writes (TOCTOU race)\n`lib/repositories/firebase/firebase_social_sharing_repository.dart:251â€“295`\n\n### 9. Friend request expiry is client-side only\n`lib/models/friend_request.dart:82` â€” no `expiresAt` in Firestore; expired requests can be accepted.\n\n### 10. Shared content sort comparator always returns 0\n`lib/repositories/firebase/base_shared_content_repository.dart:674`\n\n## Why It Matters\nFindings 1-3: Group invitation, acceptance, and removal flows are all broken. Finding 5: shared_content collection is inaccessible.\n\n## Suggested Fix\n1. Use transaction: update status + add to friendUserIds atomically\n2. Allow sender to cancel; add 'cancelled' to rules; fix Dart check\n3. Implement method body matching addFriendToCategoryInternal pattern\n4. Delete syncFriendToFirebase â€” dead code\n5. Align rules with model: use ownerId + sharedWithUserIds\n6-10. Add self-check, include groupId in events, use transactions, add expiresAt, implement sort"
        },
        {
          "findingKey": "image-upload-paths-web-platform-broken",
          "title": "Fix image pipeline: wrong Storage paths bypass security, web uploads crash on blob URL and missing compression (5 critical issues)",
          "labels": [
            "Bug",
            "security",
            "recipe"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. Avatar upload uses wrong Storage path â€” bypasses security rules\n`lib/viewmodels/user_profile_viewmodel.dart:150`\n\nUploads to `avatars/$userId/...` but security rules only allow `users/$userId/...`. Permission validation at `firebase_storage_repository.dart:96` rejects the path. All avatar uploads fail.\n\n### 2. Recipe images uploaded to wrong path â€” bypasses security\n`lib/viewmodels/recipe_form/recipe_image_manager.dart:466,894,1000` + `xfile_upload_handler.dart:174`\n\nMultiple upload calls use `recipes/$recipeId/...` instead of `users/$userId/recipes/...`. Same security rule violation.\n\n### 3. File(blob URL) on web always crashes\n`lib/viewmodels/recipe_form/recipe_image_manager.dart:888` + `xfile_upload_handler.dart:168` + `image_picker_service.dart:153`\n\nOn web, `xFile.path` is a `blob:http://...` URL. `File(xFile.path)` creates a dart:io File that doesn't exist on web. Any `.readAsBytes()` throws.\n\n### 4. flutter_image_compress not supported on web â€” all file uploads fail\n`lib/repositories/firebase/firebase_storage_repository.dart:410â€“474`\n\n`compressImage()` throws `MissingPluginException` on web. Catch returns `null`. `uploadImage()` then throws 'Failed to compress image'. Recipe image uploads on web are completely broken.\n\n### 5. uploadImageBytes has no compression â€” full-size bytes uploaded\n`lib/services/storage_service.dart:85â€“118`\n\nPasses bytes directly to repository without compression. Web avatar uploads get full-resolution images.\n\n## Why It Matters\nFindings 1-2: Avatar and recipe image uploads fail with PermissionDeniedException (or bypass security if validation is skipped). Findings 3-4: ALL image operations on web are broken.\n\n## Suggested Fix\n1-2. Fix paths to `users/$userId/avatars/...` and `users/$userId/recipes/...`\n3. On web, use `xFile.readAsBytes()` directly; never construct dart:io File\n4. Add `kIsWeb` check; skip compression on web or use web-compatible library\n5. Call `compressImage()` on bytes before upload"
        },
        {
          "findingKey": "image-display-cleanup-compression-gaps",
          "title": "Fix image display & cleanup: orphaned images on delete, fake CDN params, fullscreen OOM, double compression (10 issues)",
          "labels": [
            "Bug",
            "recipe"
          ],
          "priority": 2,
          "description": "## Finding\n\n### 1. Recipe deletion never deletes Storage images â€” permanent orphans\n`lib/services/unified/modules/service_adapters/recipe_service_adapter.dart:59â€“68`\n\n`deleteRecipe()` only deletes Firestore doc. Associated Storage images at `users/$userId/recipes/` accumulate forever.\n\n### 2. Fake transform params appended to Firebase Storage URLs\n`lib/services/performance/optimized_image_loader.dart:41â€“68`\n\nAppends `?w=X&h=Y&q=85&f=webp` to Firebase Storage URLs. Firebase doesn't support server-side transforms â€” this breaks cache keys and may break image loading.\n\n### 3. Fullscreen viewer loads full-resolution images with no dimension limits\n`lib/views/recipe_detail/fullscreen_image_viewer.dart:108â€“120`\n\nNo `cacheWidth`/`cacheHeight`. PageView preloads adjacent pages = 3 full-res images decoded simultaneously.\n\n### 4. Image.network() used in 5 places without caching\nfullscreen_image_viewer, profile_menu, 3 recipe selection dialogs â€” re-downloads on every build.\n\n### 5. Thumbnail path derivation breaks for non-.jpg files\n`lib/repositories/firebase/firebase_storage_repository.dart:499â€“501`\n\n`.replaceAll('.jpg', '_thumb.jpg')` is no-op for `.jpeg`, `.png`, `.webp` â€” thumbnail overwrites original.\n\n### 6. GDPR storage erasure failure silently swallowed\n`lib/services/account/account_deletion/storage_deletion_operations.dart:59â€“67`\n\nReturns `true` even when Storage deletion fails. User's files remain indefinitely.\n\n### 7. 5MB client vs 10MB server size limit mismatch\nClient rejects files the server would accept. Validation runs pre-compression.\n\n### 8. Double compression â€” picker at 90% quality + flutter_image_compress\nQuality loss and wasted CPU from compressing twice.\n\n### 9. maxWidth/maxHeight defined but never passed to compressor\n`firebase_storage_repository.dart:435â€“459` â€” constants exist but aren't forwarded to `FlutterImageCompress`.\n\n### 10. Storage rules allow image/gif and any image/* via client-set contentType\n`storage.rules:8â€“10` â€” SVG with embedded JS could be uploaded.\n\n## Why It Matters\nFinding 1: Unbounded Storage cost growth. Finding 2: Image loading may break. Finding 3: OOM on image carousel.\n\n## Suggested Fix\n1. Delete Storage images before Firestore doc\n2. Remove Firebase from `_supportsTransformation()`\n3. Add `memCacheWidth` based on screen size\n4. Replace with CachedNetworkImage\n5. Use path package for extension handling\n6. Return false on failure; implement retry queue\n7-9. Align limits; compress once; pass dimensions\n10. Restrict to jpeg|png|webp"
        },
        {
          "findingKey": "auth-security-verification-enumeration-ratelimit",
          "title": "Fix auth security: registration without email verification, rate limiter bypassable, user enumeration via error messages (5 critical issues)",
          "labels": [
            "security",
            "account"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. Registration allows full app access without email verification\n`lib/services/auth_service.dart:51â€“96`\n\n`registerWithEmail()` creates Firebase account and returns `true` immediately. No `sendEmailVerification()`, no `emailVerified` check, no gate screen. Users can register with any email (including others') and gain full access.\n\n### 2. Rate limiter bypassable via app restart â€” unlimited login retries\n`lib/services/auth_service.dart:87â€“95`\n\nClient-side `_rateLimiter` is in-memory only, not persisted. Killing and relaunching the app resets all token buckets. `RateLimitException` is also caught by generic `catch(e)` and swallowed.\n\n### 3. User enumeration via distinct error messages\n`lib/services/auth_service.dart:545â€“549`\n\n`user-not-found` â†’ 'No user found' vs `wrong-password` â†’ 'Wrong password'. Different messages reveal whether an email has an account.\n\n### 4. Password reset not rate-limited server-side\n`lib/repositories/firebase/firebase_auth_repository.dart:179â€“187`\n\nClient-side limiter (3 tokens, 1/hour refill) resets on restart. Attacker can spam reset emails to any address.\n\n### 5. Password reset confirms email existence\n`lib/services/auth_service.dart:205â€“217`\n\n`user-not-found` error propagated to UI reveals whether address has account. Should always show 'if that email exists, a reset link was sent'.\n\n## Why It Matters\nFinding 1: Account squatting, phishing enabled. Finding 2: Credential stuffing unmitigated. Findings 3+5: Account enumeration.\n\n## Suggested Fix\n1. Send verification email after registration; add gate screen checking emailVerified\n2. Add explicit `on RateLimitException` handler; persist bucket to SharedPreferences; add server-side rate limiting\n3. Merge user-not-found and wrong-password to same message\n4. Use Firebase App Check + server-side email limits\n5. Always show generic success message on reset"
        },
        {
          "findingKey": "auth-mfa-deletion-session-bugs",
          "title": "Fix auth MFA & account deletion: MFA removal without re-auth, partial deletion state, audit log race, password minimum (11 issues)",
          "labels": [
            "Bug",
            "account"
          ],
          "priority": 2,
          "description": "## Finding\n\n### 1. MFA unenrollment requires only confirmation dialog â€” no re-authentication\n`lib/views/settings/mfa_settings_view.dart:137â€“177`\n\nRemoving MFA factor shows only yes/no dialog. No password re-auth. Attacker with briefly-unlocked device can disable MFA with one tap.\n\n### 2. Account deletion leaves partial state on failure\n`lib/services/account/account_deletion_service.dart:152â€“162`\n\nIf `user.delete()` throws after Firestore data already deleted: data gone but auth account lives (GDPR violation). If Firestore fails: auth account stays with orphan data.\n\n### 3. requiresReauth flag silently dropped\n`lib/services/account/account_deletion_service.dart:169` sets `result['requiresReauth'] = true` but `ProfileViewModel.deleteAccount()` never checks it.\n\n### 4. Audit log written before auth deletion succeeds\n`lib/services/account/account_deletion_service.dart:135â€“162`\n\nGDPR audit log records 'deletion' before `user.delete()`. If auth delete fails, false audit trail.\n\n### 5. Sessions not invalidated on other devices after password change\n`lib/services/auth_service.dart:488â€“500`\n\nNo call to `revokeRefreshTokens`. Attacker sessions remain valid up to 1 hour.\n\n### 6. Password minimum is 6 characters\n`lib/viewmodels/auth_viewmodel.dart:306â€“308` â€” no complexity check, no breach check.\n\n### 7. MFA phone defaults silently to +46\n`lib/views/settings/mfa_settings_view.dart:67â€“68` â€” wrong number for non-Swedish users.\n\n### 8. FirebaseAuth.instance used directly in MFA methods\n`lib/services/auth_service.dart:246,261,286,343,374,422,431,472` â€” bypasses DI, untestable.\n\n### 9. GetIt.instance used instead of ServiceLocator\n`lib/views/settings/mfa_settings_view.dart:23` + `mfa_challenge_dialog.dart:48`\n\n### 10. Email TLD regex rejects >4 char TLDs\n`lib/viewmodels/auth_viewmodel.dart:283â€“289` â€” `.museum`, `.photography` blocked.\n\n### 11. Loading spinner stuck on re-auth failure\n`lib/widgets/common/profile/handlers/auth_action_handler.dart:57â€“78` â€” `Navigator.pop` skipped if `context.mounted` is false.\n\n## Why It Matters\nFinding 1: MFA security undermined. Finding 2: GDPR Article 17 compliance gap. Finding 5: Stolen sessions survive password change.\n\n## Suggested Fix\n1. Require password re-auth before unenrollMfa\n2. Delete auth first; queue Firestore cleanup via Cloud Function\n3. Check requiresReauth and show re-auth prompt\n4. Move audit log after user.delete() succeeds\n5. Call revokeRefreshTokens via Cloud Function on password change\n6-11. Raise minimum to 8; force int'l phone format; add MFA to AuthRepository; fix regex; dismiss spinner in finally"
        },
        {
          "findingKey": "shopping-stubs-menu-generation-bugs",
          "title": "Fix shopping & menu: clearCompleted/uncheckAll/export stubs, isLoading stuck, menu quantity corruption, recipe reuse (12 issues)",
          "labels": [
            "Bug",
            "shopping",
            "menu"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. clearCompletedItems() and uncheckAllItems() are no-op stubs\n`lib/services/unified/modules/shopping_item_management_module.dart:305â€“306`\n\nBoth return `true` but do nothing. Clear-bought and uncheck-all buttons silently succeed without effect.\n\n### 2. addItemsFromRecipe is a stub â€” always returns true\n`lib/services/unified/modules/shopping_item_management_module.dart:307â€“312`\n\nNo items added via this code path.\n\n### 3. isLoading is `final false` â€” loading spinners never show\n`lib/services/unified/unified_shopping_service.dart:146,183`\n\n`final bool _isLoading = false` never mutated. `isSyncing` also missing.\n\n### 4. exportListAsText returns hard-coded mock string\n`lib/services/unified/modules/shopping_list_management_module.dart:284`\n\nUsers sharing shopping list get 'Mock shopping list export'.\n\n### 5. Menuâ†’shopping conversion corrupts quantities\n`lib/widgets/common/input/shopping_list_selector.dart:391â€“422`\n\nUses full raw ingredient string (including quantity) as dedup key. '2 dl mjÃ¶lk' and '3 dl mjÃ¶lk' are separate items. Exact duplicates get `amount += 1` instead of summing parsed quantities.\n\n### 6. Regenerating menu section can reuse recipes from other sections\n`lib/viewmodels/menu/menu_generator.dart:99â€“118`\n\n`regenerateMenuSection` doesn't exclude recipes already in the menu. Same recipe can appear in Lunch and Middag.\n\n### 7. Empty recipe bucket silently drops menu section\n`lib/services/menu_service.dart:93â€“101`\n\nIf no recipes match a requested meal type, section silently omitted. No error shown.\n\n### 8. No dedup when adding menu to existing shopping list\n`lib/widgets/common/input/shopping_list_selector.dart:300â€“351`\n\nMenu items appended without checking if they already exist. Tapping twice doubles all items.\n\n### 9. toggleItemBought rollback uses mutated list + stale listIndex\n`lib/services/unified/modules/shopping_item_management_module.dart:276â€“283`\n\n### 10. firstWhere throws StateError if item not found\n`lib/models/unified/unified_shopping_list.dart:521`\n\n### 11. Parse fallback adds '1 st' to every unparseable ingredient\n`lib/utils/text/shopping_list_generator.dart:265â€“274`\n\n### 12. formattedAmount uses English decimal '1.5' not Swedish '1,5'\n`lib/models/unified/unified_shopping_item.dart:322â€“330`\n\n## Why It Matters\nFindings 1-4: Core shopping features (clear, uncheck, export) are non-functional stubs. Finding 5: Shopping lists from menus have wrong quantities.\n\n## Suggested Fix\n1-2. Implement method bodies using existing addItemsBatchToActiveList pattern\n3. Make _isLoading non-final; set true/false around async ops\n4. Implement using ShoppingAnalyticsManager.exportListAsText pattern\n5. Use IngredientProcessor.parseAndNormalize for quantity extraction and merging\n6. Pass currentMenu to exclude already-used recipes\n7. Return error listing which meal types couldn't be filled\n8. Check existing items by normalized name before appending\n9-12. Capture listId not index; add orElse; show name without unit for qty-less items; use toSwedishHalfFraction"
        },
        {
          "findingKey": "onboarding-flow-broken-non-atomic",
          "title": "Fix onboarding: non-atomic completion, error swallowed, registration bypasses flow, FCM permission fires too early (12 issues)",
          "labels": [
            "Bug",
            "account"
          ],
          "priority": 1,
          "description": "## Finding\n\n### 1. completeOnboarding is not atomic â€” two separate Firestore writes\n`lib/viewmodels/onboarding_viewmodel.dart:79â€“122`\n\nAllergen save and onboarding flag are separate writes. App kill between them = allergen data saved but onboarding re-shown, discarding selections.\n\n### 2. markOnboardingComplete silently swallows exceptions\n`lib/services/user_service.dart:502â€“519`\n\nCatch logs but doesn't rethrow. Caller navigates to home thinking success. Next launch re-shows onboarding.\n\n### 3. Allergen save failure doesn't prevent markOnboardingComplete\n`lib/viewmodels/onboarding_viewmodel.dart:83â€“96`\n\nReturn value of `updateAllergenPreferences` not checked. Proceeds to mark complete even if allergens weren't saved.\n\n### 4. Profile null â†’ skips onboarding, shows main app\n`lib/main.dart:806â€“812`\n\n`profile == null` causes code to fall through to `MinaReceptView`. New users see main app flash before onboarding.\n\n### 5. Registration navigates directly to MinaReceptView, bypassing onboarding\n`lib/views/auth_view.dart:512â€“520`\n\nImperative `pushReplacement` to MinaReceptView on registration success, before AuthWrapper can check onboarding flag.\n\n### 6. FCM permission dialog fires during bootstrap â€” before onboarding\n`lib/services/notifications/fcm_service.dart:94â€“127`\n\nPermission requested during DI bootstrap. On iOS this is the one-time system dialog â€” fires without context or explanation.\n\n### 7. Import buttons navigate away without completing onboarding\n`lib/views/onboarding/onboarding_import_page.dart:41â€“53`\n\nUser can import a recipe and close the app without 'SlutfÃ¶r' being called. Onboarding not marked complete.\n\n### 8. Deep link processed during onboarding\n`lib/main.dart:720â€“726`\n\nNo check for `hasCompletedOnboarding` before processing deep link. Navigation pushed on top of onboarding.\n\n### 9. registerWithEmail doesn't create Firestore profile\n`lib/services/auth_service.dart:51â€“96`\n\nProfile creation is async side-effect of auth stream. Window between auth creation and profile exists.\n\n### 10. New profile doesn't set hasCompletedOnboarding explicitly\n`lib/services/user_service.dart:120â€“134` â€” relies on model default.\n\n### 11. PageView allows unrestricted swiping past boundaries\n`lib/views/onboarding/onboarding_view.dart:62â€“70`\n\n### 12. Skip button visible on last page â€” confusing UX\n`lib/views/onboarding/onboarding_view.dart:81â€“106`\n\n## Why It Matters\nFindings 1-5: Onboarding flow has multiple bypass paths â€” new users can reach main app without completing setup. Finding 6: iOS notification permission wasted.\n\n## Suggested Fix\n1. Merge allergen save + onboarding flag into single Firestore update\n2. Rethrow or return bool from markOnboardingComplete\n3. Check return value before proceeding\n4. Show loading spinner when profile is null\n5. Remove imperative navigation; trust AuthWrapper\n6. Defer FCM permission to post-onboarding\n7. Mark onboarding complete before navigating to import\n8. Check hasCompletedOnboarding before processing deep links\n9. Create profile atomically in registration flow\n10-12. Set explicitly; use NeverScrollableScrollPhysics; hide Skip on last page"
        }
      ]
    },
    {
      "scanFocus": "creative-scan-19-ux-architecture-features-modernization-social",
      "tickets": [
        {
          "findingKey": "rebuild-social-system-3-collections",
          "title": "Rework social system: reduce 14 Firestore collections to 3, eliminate 35K+ lines of scaffolding",
          "labels": [
            "tech-debt",
            "social"
          ],
          "priority": 2,
          "description": "## Opportunity\nThe social system spans 120+ files and ~40,000 lines of code across 14 Firestore collections, with 7-8 abstraction layers for a simple friend-add. The complexity generates bugs faster than they can be fixed — BUG-016/017/018 are all workarounds for architectural issues, not root cause fixes. Patching is estimated at 6-8 weeks with debt remaining; rebuilding is 3-4 weeks and eliminates the debt.\n\n## Current State\n- 14 Firestore collections (friend_requests, group_invitations, shared_content, shared_recipes, shared_menus, shared_shopping_lists, shared_personal_tags, public_profiles, users/{id}/friends, users/{id}/friendCategories, users/{id}/categoryMemberships, plus 3 denormalized variants)\n- 90+ service/operations files under unified/friends/ and unified/operations/\n- FriendCategory conflates \"contact organizer\" and \"group\" concepts\n- Empty stub classes (FriendsSyncService, FriendsPresenceService, FriendsCacheService)\n- Hardcoded mock-user-id in production permission check (FriendsViewModel:327)\n- acceptGroupInvitation calls updateSentInvitation instead of updateReceived\n\n## Proposed Improvement\nReplace with 3 Firestore collections: `connections` (friend requests + friendships), `groups` (with inline memberIds and pendingInvites), `shares` (all content types). 3 services instead of 50 files. Server-enforced Firestore rules instead of application-layer permission checks. Deep link sharing with `linkToken` field on shares.\n\nReference: Samsung Food uses a flat sharing model. Cookpad uses simple follow/collection patterns. Neither needs 14 collections for their social features.\n\n## Effort vs Impact\nMajor undertaking (3-4 weeks rebuild + 1 week migration). But net faster than patching (6-8 weeks). Eliminates entire classes of bugs and removes ~35,000 lines of code. The single highest-impact architectural improvement available."
        },
        {
          "findingKey": "di-container-session-scope-boundary",
          "title": "Rework DI container: add user-session scope to fix cross-user cache leaks and singleton lifecycle bugs",
          "labels": [
            "tech-debt",
            "backend"
          ],
          "priority": 1,
          "description": "## Opportunity\nThe DI container registers everything as app-scoped singletons, but many services hold user-specific state. On logout/login-as-different-user, singletons retain stale data from the previous user. This is the root cause behind BUT-32 (PersonalTagViewModel stale data), BUT-68 (NotificationService not registered), BUT-88 (ChangeNotifierProvider disposes shared singletons), BUT-105, and BUT-106 (cross-user cache leaks). Fixing each individually does not address the systemic absence of a lifecycle boundary.\n\n## Current State\n- Single GetIt scope for all services (app-scoped)\n- NotificationService requires userId but DI container is app-scoped — cannot register correctly\n- 40 ViewModels with 118 ServiceLocator.get calls, no compile-time registration guarantees\n- Health check declares success even when services are unregistered (BUT-68)\n- No auth-state-change teardown mechanism for user-scoped services\n\n## Proposed Improvement\nTwo-tier DI: (1) app-level GetIt scope for global services (Firebase instances, repositories, auth), (2) per-session GetIt scope created on login, torn down on logout, for all user-scoped services and ViewModels. This eliminates the stale-singleton-across-sessions class of bug entirely. NotificationService lives in session scope and is properly disposed on logout.\n\nReference: Riverpod's ProviderScope/ProviderContainer offers this model natively, but the same can be achieved with two GetIt instances.\n\n## Effort vs Impact\nMajor (3-4 weeks). Addresses the root cause running through 6+ existing tickets. Without this, every user-scoped service will continue to independently re-implement auth-state-change cleanup, and new bugs of this class will keep appearing."
        },
        {
          "findingKey": "unified-service-layer-rework-90-files",
          "title": "Rework unified service layer: replace 90-file facade with 4 focused, stream-based services",
          "labels": [
            "tech-debt",
            "recipe",
            "backend"
          ],
          "priority": 2,
          "description": "## Opportunity\nThe unified service layer (lib/services/unified/) spans 90 files and ~30,000 lines. It accounts for or enables ~25 of the 122 filed tickets. The original monolith was decomposed into facade + operations + modules + helpers + coordinators, but the decomposition created circular references, non-deterministic initialization, and state ownership split across the facade and all its helpers.\n\n## Current State\n- Three parallel delegation chains for the same concept (focused modules AND legacy feature interfaces)\n- Initialization uses exceptions for control flow (catching LateInitializationError on late final fields)\n- Platform forking (kIsWeb) woven through 8+ files as BUG-003 patches\n- _recipes list mutated from 6+ different contexts with notifyListeners callbacks from each\n- 90 files for 4 features (personal CRUD, social sharing, realtime editing, shopping)\n\n## Proposed Improvement\n4 focused services that each own their own state: RecipeRepository (single source of truth via streams), SharingService (social/collaborative), RealtimeEditingService (realtime sessions), RecipeCacheService (persistence). ViewModels subscribe to streams rather than getting callbacks. Platform branch lives in one place: repository's initialize() method.\n\n## Effort vs Impact\nMajor (4-6 weeks, can be done incrementally). The highest ticket-density area in the codebase. Each future bug fix in this layer takes longer than it should because of the abstraction depth."
        },
        {
          "findingKey": "drag-drop-meal-calendar-veckomeny",
          "title": "Add drag-and-drop weekly meal calendar to replace flat menu list",
          "labels": [
            "idea",
            "menu"
          ],
          "priority": 2,
          "description": "## Opportunity\nThe app is named around \"veckomeny\" (weekly menu) but the current view is a flat list of recipes grouped by meal type, not by day. There is no concept of Monday/Tuesday/Wednesday, no calendar UI, no ability to assign pasta to Wednesday and fish to Friday. This is the biggest gap between the app's stated purpose and its implemented feature set.\n\n## Current State\nVeckomenyView shows week number as a cosmetic badge. Menu is Map<String, List<Recipe>> keyed by category (\"Middag\"), not by date. Users cannot track what they planned for today or auto-generate a shopping list for \"this week's dinners\" with confidence.\n\n## Proposed Improvement\nDay-based Map<DateTime, List<MenuEntry>> model. GridView calendar (Mon-Sun) with Flutter's LongPressDraggable + DragTarget for recipe assignment. Integrate with shopping list generation (\"generate list for selected days\"). Show today's meals prominently on the home screen.\n\nReference: Paprika has a calendar grid with drag-and-drop. Mealime shows weekly calendar with per-day slots. Both set user expectations for this feature.\n\n## Effort vs Impact\nMedium effort (model change + calendar widget + migration of save/load). Very high user impact — this is the core feature the app's name promises."
        },
        {
          "findingKey": "cooking-timers-step-progression",
          "title": "Add cooking timers and step progression to cooking mode",
          "labels": [
            "idea",
            "recipe"
          ],
          "priority": 2,
          "description": "## Opportunity\nCooking mode has a solid foundation (landscape split-view, wakelock, portion scaling) but is missing the two features users need most during active cooking: timers and step tracking. Without timers, users switch to the clock app mid-cooking. Without step progression, all instructions compete for attention equally.\n\n## Current State\nCookingModeViewModel manages only portion scaling (43 lines). No timer state, no countdown widget, no per-step highlighting. All instructions shown as a scrollable list simultaneously. No ingredient check-off capability.\n\n## Proposed Improvement\n1. Parse durations from instruction text (\"koka i 8 minuter\" -> 8-min timer) using Swedish time patterns\n2. Add CookingTimer model with countdown overlay on instructions panel\n3. Add currentStepIndex to ViewModel with prev/next buttons and visual highlighting of active step\n4. Add ingredient check-off (Set<int> checkedIngredients, toggle on tap, render with strikethrough)\n\nReference: Mealime auto-suggests timers from step text. Kitchen Stories has voice prompts. Even within Butlery's split-view design, step highlighting reduces cognitive load.\n\n## Effort vs Impact\nQuick-medium win. Timer + step progression can be built incrementally. The cooking mode VM is small and self-contained. High daily-use impact for anyone who actually cooks with the app."
        },
        {
          "findingKey": "system-share-sheet-external-sharing",
          "title": "Add system share sheet for sharing recipes outside the app via deep links",
          "labels": [
            "idea",
            "social"
          ],
          "priority": 2,
          "description": "## Opportunity\nThere is no way to share a recipe with someone who is not already a Butlery friend. DeepLinkService parses recipeShare deep links, but nothing generates a shareable URL for the system share dialog. This is the single highest-impact missing social feature — it blocks organic growth entirely.\n\n## Current State\nUniversalShareDialog only shows in-app friends/groups as share targets. No \"copy link\" or \"share via WhatsApp/SMS\" option. Deep link types are defined (recipeShare, menuShare, shoppingListShare, friendInvitation) but only parsed on receive, never generated for sending.\n\n## Proposed Improvement\nGenerate deep link URL for any shared content -> share via system share sheet (share_plus already in pubspec.yaml). Cloud Function creates short URL at butlery.app/r/{linkToken}. Non-Butlery recipients see recipe preview in browser with install prompt. Add \"copy link\" and \"share externally\" options to UniversalShareDialog alongside existing friend/group targets.\n\nReference: Samsung Food and Cookpad both support link-based sharing. This is table stakes for any social feature in 2026.\n\n## Effort vs Impact\nMedium effort (deep link generation + Cloud Function + share sheet integration). Very high growth impact — enables organic user acquisition through recipe sharing."
        },
        {
          "findingKey": "reverse-ingredient-search-what-can-i-cook",
          "title": "Add reverse ingredient search: what can I cook with what I have?",
          "labels": [
            "idea",
            "recipe"
          ],
          "priority": 3,
          "description": "## Opportunity\nUsers cannot enter ingredients they have and get matching recipes. Search is forward-only (recipe -> ingredients). Butlery already has ingredientsNormalized on every recipe, making reverse lookup technically feasible without a new data pipeline. Combined with Butlery's allergen auto-tagging, this could be uniquely powerful: \"what can I cook with what I have, safe for my allergens.\"\n\n## Current State\nNo reverse ingredient lookup exists. ingredientsNormalized field is stored on RecipeCore but only used for forward search matching. No \"I have these ingredients\" UI.\n\n## Proposed Improvement\nUI: ingredient chip input (\"I have: [potatis] [lax] [gradde]\"). Service: score recipes by ingredient match percentage against user's input, filtered by allergen preferences. Display: ranked results with match percentage and missing ingredients list. Local-only search initially (no cloud needed).\n\nReference: Yummly's \"What's in your fridge?\" is a flagship feature. Supercook.com is entirely built around this concept.\n\n## Effort vs Impact\nMedium effort (UI + scoring service leveraging existing normalized data). High differentiator potential, especially combined with allergen awareness that competitors lack."
        },
        {
          "findingKey": "ai-personalization-cooking-history",
          "title": "Add AI recipe personalization from cooking history and ratings",
          "labels": [
            "idea",
            "recipe",
            "menu"
          ],
          "priority": 3,
          "description": "## Opportunity\nRecipeCore already stores lastCookedAt, rating, averageRating, and ratingCount — the raw data for taste learning exists but is never fed back into ranking or suggestions. MenuService is explicitly documented as \"NOT AI/LLM-based\" and does simple random selection. No \"because you cooked X, try Y\" logic exists.\n\n## Current State\nMenuGenerator filters by meal type and allergens, then selects randomly with a variety check. No taste learning loop. No recency weighting. No rating influence on suggestions.\n\n## Proposed Improvement\nBuild a lightweight local scorer in MenuService that weights recipes by: recency of last cooked (avoid repetition), personal rating (surface favorites), tag overlap with highly-rated recipes (taste profiling), and seasonal ingredients. No cloud ML needed — a deterministic score function covers 80% of the value. Apply to both menu generation and a new \"suggested for you\" section.\n\nReference: Deglaze and Whisk learn user preferences. The data is already collected in Butlery — it just needs the feedback loop.\n\n## Effort vs Impact\nMedium effort (scoring function + integration with menu generator + suggested section). High differentiation — knowing a Swedish household's actual cooking habits is a unique advantage."
        },
        {
          "findingKey": "presence-service-wrong-backend-rtdb",
          "title": "Rework presence service: replace Firestore polling with Firebase Realtime Database onDisconnect",
          "labels": [
            "tech-debt",
            "backend"
          ],
          "priority": 3,
          "description": "## Opportunity\nThe presence service uses Firestore, which does not support onDisconnect. The service's own comment acknowledges this. The workaround is a 2-minute heartbeat timer that writes to Firestore per online user (expensive) and still shows users as online for up to 2 minutes after they close the app. Three tickets (BUT-39 polling patterns, BUT-112 timer leaks, BUT-72 orphan presence) trace directly to this design mismatch.\n\n## Current State\n411-line service with: heartbeat timer every 2 minutes (expensive), _setupDisconnectHandler() that is a no-op (Firestore has no onDisconnect), asyncMap with await-for-take(1) that can deadlock on empty batch streams, timer leaks on auth change (no listener for sign-out).\n\n## Proposed Improvement\nReplace with Firebase Realtime Database for the single presence/{userId} path. RTDB has native onDisconnect support, eliminating the heartbeat timer entirely. The multi-batch stream merge becomes a single RTDB query. Typing indicator timers remain but RTDB onDisconnect handles cleanup on app crash.\n\n## Effort vs Impact\nQuick win (1 week). Eliminates 3 bug classes permanently. Adding RTDB alongside Firestore for this one use case is straightforward."
        },
        {
          "findingKey": "dart3-quick-wins-switch-expressions-pwa-manifest",
          "title": "Adopt Dart 3 switch expressions across 40+ enum files and fix PWA manifest branding",
          "labels": [
            "tech-debt",
            "recipe"
          ],
          "priority": 4,
          "description": "## Opportunity\nTwo low-risk quick wins that improve code quality and web presence:\n\n1. Switch expressions: 40-50 enum extension files use classic switch statements instead of Dart 3 switch expressions. Switch expressions enable exhaustiveness checking without default cases, catching missed enum variants at compile time.\n\n2. PWA manifest: web/manifest.json still has Flutter defaults — name \"butlery\", description \"A new Flutter project.\", theme color #0175C2 (Flutter blue). index.html meta description also says \"A new Flutter project.\"\n\n## Current State\n- Enums like ConditionType, TierResult, ImportResultV2 use switch (this) { case X: return ...; default: ... } pattern\n- web/manifest.json has placeholder content with no categories, screenshots, or correct branding\n- very_good_analysis is in dev_dependencies but analysis_options.yaml includes flutter_lints instead\n\n## Proposed Improvement\n1. Migrate enum extensions to switch expressions (mechanical, file-by-file, 1-2 days)\n2. Update manifest.json: branded name, Swedish description, forestGreen theme color, food/lifestyle categories\n3. Bonus: activate very_good_analysis ruleset (one-line change + lint fix sweep, 3-5 days)\n\n## Effort vs Impact\nItems 1+2 are trivial (<2 days combined). Item 3 is medium (3-5 days for lint fixes). All zero-risk at runtime. Switch expressions add compile-time safety; manifest fixes install UX."
        },
        {
          "findingKey": "startup-optimization-manager-fake-delays",
          "title": "Remove StartupOptimizationManager fake delays adding 2-3 seconds to app startup",
          "labels": [
            "performance",
            "backend"
          ],
          "priority": 1,
          "description": "## Finding\nStartupOptimizationManager (lib/services/performance/startup_optimization_manager.dart, 520 lines) is registered in DI and runs on every app launch. Its service initializer methods (_initializeFirebase, _initializeAuth, etc.) all consist of comments followed by await Future.delayed(Duration(milliseconds: 500)). No real initialization logic exists — the delays are stubs. StartupMetrics tracks timing data that measures only the hardcoded delays. The real bootstrap sequence already exists in lib/core/bootstrap/stages/.\n\n## Why It Matters\nAdds 2-3 seconds of artificial delay to every app startup in production. Users experience a noticeably slower launch for no reason.\n\n## Suggested Fix\nDelete the entire file and remove the DI registration in performance_module.dart. The real bootstrap sequence in lib/core/bootstrap/stages/ handles actual initialization. Verify no other code depends on StartupOptimizationManager before removing."
        },
        {
          "findingKey": "shopping-list-stubs-clear-uncheck-noop",
          "title": "Fix shopping list clearCompletedItems and uncheckAllItems stubs that silently do nothing",
          "labels": [
            "bug",
            "shopping"
          ],
          "priority": 2,
          "description": "## Finding\nIn shopping_item_management_module.dart (lines 305-312), clearCompletedItems() and uncheckAllItems() are stub methods that always return true without performing any action. Users who tap \"clear completed\" or \"uncheck all\" get no error but nothing happens.\n\n## Why It Matters\nSilent no-ops are worse than missing features — the user thinks the action succeeded. This affects daily shopping list usage.\n\n## Suggested Fix\nImplement clearCompletedItems() to filter items where isChecked == true and batch-delete them. Implement uncheckAllItems() to batch-update all items setting isChecked = false. Both should use Firestore batch writes for atomicity."
        }
      ]
    },
    {
      "scanFocus": "creative-ultrathink-scan-20-tagging-analytics-import-settings-offline-onboarding",
      "tickets": [
        {
          "findingKey": "account-deletion-ui-not-wired-gdpr",
          "title": "Wire account deletion UI: AccountDeletionService exists but no route or button reaches it",
          "labels": [
            "bug",
            "account"
          ],
          "priority": 1,
          "description": "## Finding\nAccountDeletionService is comprehensive (deletes 20+ data categories, creates audit log, handles requires-recent-login). But no route exists in Routes and no UI entry point exists in the profile sheet or account security view. The service is built but the door is locked.\n\n## Why It Matters\nGDPR Article 17 right-to-erasure requires users to be able to delete their account. This is a compliance gap, not a feature gap.\n\n## Suggested Fix\nAdd a 'Danger Zone' collapsible section at the bottom of account_security_view.dart with a 'Delete Account' button that calls AccountDeletionService. Requires re-authentication confirmation dialog. The service already handles all data deletion logic."
        },
        {
          "findingKey": "cooking-streak-gamification-engine",
          "title": "Add cooking streak and milestone system using existing ActivityType.achievementUnlocked infrastructure",
          "labels": [
            "idea",
            "recipe"
          ],
          "priority": 2,
          "description": "## Opportunity\nThe 'Lagat idag' button is the central behavioral signal but it leads nowhere. ActivityType.achievementUnlocked and milestoneReached are defined in the enum and referenced in ActivityFeedItem.description with l10n strings, but zero achievement logic exists. No streak computation anywhere.\n\n## Current State\nlastCookedAt stores per-recipe timestamp. No user-level 'cooked today' state, no streak counter. The 'Lagat idag' chip shows no visual state change after tapping (same outline button). No cook count displayed anywhere.\n\n## Proposed Improvement\n1. Add UserStats Firestore document: currentStreak, longestStreak, lastCookedDate, totalCookedCount\n2. On markAsCooked: check lastCookedDate — if yesterday, increment streak; if today, no-op; if older, reset to 1\n3. Display streak as flame icon + count in recipe list header\n4. Milestones: 3/7/30-day streak, 5/10/50 recipe milestones, 5+ cuisine diversity — fire ActivityType.milestoneReached\n5. Fix 'Lagat idag' chip: change to green filled state with checkmark after tapping, show lastCookedText on recipe cards\n\nReference: Duolingo's primary retention driver is the streak. A cooking streak is directly applicable.\n\n## Effort vs Impact\nMedium effort (3-4 days). Very High retention impact. The single highest-leverage gamification feature."
        },
        {
          "findingKey": "tag-discovery-view-browsable-catalogue",
          "title": "Add tag discovery view: browsable auto-tag catalogue with recipe counts per tag",
          "labels": [
            "idea",
            "tagging"
          ],
          "priority": 2,
          "description": "## Opportunity\nThe auto-tag system generates 30+ tag types (cuisine, mood, season, difficulty, texture, sustainability) but users never see them as a browsable catalogue. Tags are only visible as individual badges on recipe detail. A user who imports 50 recipes has no idea the system already classified them by cuisine, season, and dietary status.\n\n## Current State\nTagResult.tags is a Set<String> on each recipe. TagDisplayUtils has priority ordering. PersonalTagsView only shows user-created tags. No aggregation view exists. getTagsByUsage() exists for personal tags but no equivalent for auto-tags.\n\n## Proposed Improvement\nA 'Tag Overview' entry point accessible from MinaReceptView showing:\n- Scrollable tag cloud organized by section (Allergens, Dietary, Cuisine, Mood, Season, Difficulty)\n- Recipe count per tag ('8 vegetariska', '3 thai-recept')\n- Tap any tag to immediately filter the recipe list\n- 'Seasonal Now' highlight section showing recipes matching current season\n\nThe data already exists on every recipe. Zero new Firestore queries needed — pure client-side aggregation.\n\n## Effort vs Impact\nMedium effort (1 new view + aggregation). High impact — makes invisible classification work visible. The tagging backend is architecturally ahead of its UX."
        },
        {
          "findingKey": "daily-habit-loop-push-notification",
          "title": "Implement daily 17:00 push notification habit loop with recipe suggestion",
          "labels": [
            "idea",
            "recipe"
          ],
          "priority": 2,
          "description": 