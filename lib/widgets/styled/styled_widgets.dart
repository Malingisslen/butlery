/// Styled widgets barrel for `StyledCard` and `StyledInput`.
///
/// `StyledButton` (and its `StyledButtons` helper) were removed in BUT-867
/// after the last call-site migrated to `ActionButtons` in
/// `lib/widgets/common/buttons/action_buttons.dart`. Use that family for all
/// new code — see `lib/widgets/common/buttons/README.md` for the canonical
/// hierarchy.

export 'styled_card.dart';
export 'styled_input.dart';
