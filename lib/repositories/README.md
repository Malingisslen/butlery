# repositories

This directory defines repository interfaces and their implementations used throughout the app.

Subfolders:

- `interfaces/` – abstract classes describing repository contracts.
- `firebase/` – implementations backed by Firebase services.
- `hive/` – local storage implementations using Hive.
- `mock/` – mock repositories for testing and development.
