# Mock Repositories

This folder contains simple in-memory implementations of the repository
interfaces. They are intended for unit tests and development environments
where connecting to Firebase is undesirable.
